# 第8周第一阶段 正式输入核定与数据准备验收

本阶段依据：最新版实习需求Day40.1；现有scripts/step1_data_prep.py、train_pipeline.py及reports/week8/data_statistics.json。2026-09-14按分阶段执行要求细化。

目标：确定本周正式数据来源、形成可追溯的9:1训练/验证集，并证明训练框架实际读取的数据符合预期。任务1–3已于2026-09-14完成，任务4–5已于2026-09-15完成，任务6已于2026-09-15完成归档；本阶段按步骤推进，不启动完整SFT、DPO或蒸馏。

## 为什么从这里开始

已有本地结果为Day6归档5000条→4999条→4499train/500validation；但这是旧默认输入的一次技术验证，不代表已决定本周最终使用这批数据。近期恢复工作另有修订、筛选及1580条训练/83条验证的记录，需要先检查完整血缘及适用范围。不得混用旧版本、恢复版本或把历史验证集直接并回新训练集。

此外，本地统计针对ShareGPT完整Chat Template计算长度，而旧训练入口选择Alpaca格式（任务3现已将数据登记改为ShareGPT，真实loader已于任务5验收）；两种表示的实际模板化长度和监督位置已用LLaMA-Factory核对。仅字段存在、分数复算或字符模式quick通过不足以证明训练输入正确。

## 任务1 盘点并核定输入

- [x] 盘点原始5000条、Day7清洗4999条、近期审核修订数据、1580/83恢复实验输入。
- [x] 记录每份文件的生成步骤、来源、条数、内容哈希、审核结论、训练/开发/验证/最终测试角色。
- [x] 对照最近修订是否已经落实到训练文件，区别“格式合格”“审核通过”“生成质量已验证”。
- [x] 形成候选差异表和本周输入建议；若历史使用意图存在实质冲突，先提出具体选择，再冻结最终版本。

产出：输入版本对照表、机器可读输入清单。验收：每个本周输入可定位到唯一文件与哈希，没有将诊断或保留测试材料混入训练。

## 任务2 固定处理与评估协议

- [x] 固定输入版本、tokenizer身份及Chat Template，seed42、2048-token限制。
- [x] 固定清洗→长度处置（超长整条隔离、不截断正文）→去重→问题分组→9:1划分顺序，以及实际计数取整规则。
- [x] 明确已有83条保留集及其他诊断/正式评估题的处理；需要保留隔离的集合继续隔离，不重新随机分配。
- [x] 确定训练采用Alpaca或ShareGPT的具体表示，另一种保留为对齐交付；多轮和system语义不得默默丢失。
- [x] 冻结本周评估方案的必要定义；不能等训练后根据结果挑评估题或成功条件。

产出：本周数据协议和排除清单。验收：规则、版本、使用角色在正式训练前确定。

## 任务3 执行全量准备与机器校验

- [x] 在独立运行目录用真实tokenizer执行处理，不覆盖已生成结果。
- [x] 检查双格式sample_id对应、角色顺序、非空回答、长度分布和截断记录。
- [x] 检查训练/验证的重复与近重复，并检查与保留评估集的污染。
- [x] 对相同输入和配置进行一次确定性复跑，比较语义产物哈希。
- [x] 报告每步数量变化、按来源分布、拒绝原因和实际划分比例。

产出：四份数据JSON、dataset_info.json、统计、处理审计、无交叉/污染检查结果和哈希。验收：所有机器检查通过，异常有逐条说明。

## 任务4 分层抽查内容

- [x] 选择约30–50条，覆盖来源、短/长样本、多轮、代码、数学及被清理/截断的样本；不足的类别全查。
- [x] 检查清理是否破坏代码/公式、截断是否留下残缺答案、多轮上下文与最终答案是否对应。
- [x] 将模型辅助审查与真实人工审核区分；未人工审核不能标成人工通过。
- [x] 对发现的问题修复处理规则或隔离样本，然后重跑受影响检查并更新版本。

产出：抽查记录、问题清单、处理结论。验收：没有悬而未决的格式或上下文错配；抽查不外推为全量事实正确。

## 任务5 验证LLaMA-Factory实际加载

- [x] 在目标Python环境核对版本；使用真实dataset_info和计划训练配置加载正式数据。
- [x] 核对实际样本数、input_ids、attention_mask、labels及监督token数量。
- [x] 重点检查Alpaca/ShareGPT表示差异、system/多轮内容，以及是否发生二次截断或无有效监督。
- [x] 记录加载器消费的数据文件哈希和有效配置，避免只验证独立清洗脚本。

产出：真实loader检查日志及代表样本可读对照。验收：框架读到的输入、回答监督和统计与协议一致。该步骤尽量CPU完成；不需要长时间GPU训练。

## 任务6 冻结本阶段交付

- [x] 汇总输入、输出、配置、tokenizer和校验记录的清单。
- [x] 给出数据阶段PASS或具体失败项，不把待查写成通过。
- [x] 明确下一阶段GPU环境、原始SFT基座和短试跑输入；完整训练使用单独输出目录。

本阶段结束条件：正式数据来源已确定、数据处理与隔离通过、抽查已记录、真实loader通过。随后进入Day40-2“GPU预检与少量步骤试跑”，再分别运行完整SFT、SFT合并验证、DPO和DPO合并验证。

## 后续阶段顺序

1. Day40-1：本文件所列的数据准备验收。
2. Day40-2：GPU/模型身份预检，SFT和DPO各做约2–5个optimizer steps技术试跑；短试跑不宣称质量改善、不继承到正式训练。
3. Day40-3：正式SFT，完成后单独验收合并和加载；接着正式DPO并单独验收。
4. Day41：新模型真实评估，再做部署监督器联调。
5. Day42：核验历史Week4教师、原始0.5B学生及冻结比较协议，完成两轮蒸馏和前后比较。该分支不必等新DPO质量通过，可按GPU预算单独安排。
6. Day43–45：补齐实测报告、检查模型备份、最后处理清理与发布。

按用户要求逐项推进；当前下一步为Day40-2预检，不将剩余任务自动串成完整训练。

## 任务1–2完成记录（当时状态）

输入核定见[输入版本决策](week8_data_input_decision.md)，具体处理及评估边界见[数据协议](week8_data_protocol.md)。机器协议位于`configs/week8_data_protocol.json`，只读审计和34项输入检查结果位于`reports/week8/phase1/`。未运行任务3–6；现有默认脚本尚未接入该协议。

## 任务3完成记录（当时状态）

已完成协议入口接入、两次独立目录全量准备及独立导出审计。实际1422train/158validation，1580条正文不变；504条去重保留题词面命中0；15份JSON两次字节哈希完全一致。详见[任务3结果](week8_phase1_task3_result.md)。下一步为任务4分层内容抽查，任务5–6仍未开始；不能因机器检查通过直接训练。

## 任务4完成记录（当时状态）

2026-09-15完成50条、69个助手回答的AI辅助逐条抽查，覆盖全部13条多轮。41条保留、9条带提示保留，13项定向代码/数值检查通过，无新增阻断项；数据和协议未改动。完整依据见[任务4结果](week8_phase1_task4_result.md)。任务5真实loader及任务6发布仍待执行。

## 任务5完成记录（当时状态）

2026-09-15在独立CPU环境使用LLaMA-Factory 0.9.3真实加载1422/158条，全部token、labels、attention_mask与独立预期一致；13条多轮、1599个助手回答共94298个监督token。Alpaca真实加载逐条等价，显式system合成检查通过。最终证据使用已绑定原始Qwen配置的attempt-02，详见[任务5结果](week8_phase1_task5_result.md)。任务6及正式训练前GPU环境/基座身份复验仍待执行。

## 任务6完成记录

2026-09-15重新核对前5步并完成交付冻结。75项汇总检查、27项回归测试、13项样例执行通过，真实CPU加载全量复跑一致。本阶段数据准备PASS；benchmark运行锁、新增题目污染检查和GPU/完整基座权重预检仍待完成，训练放行保持关闭。详见[任务6结果](week8_phase1_task6_result.md)。
