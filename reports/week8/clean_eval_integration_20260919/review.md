# 独立代码审查处理记录

审查者：本任务的独立只读代码审查子代理（按requesting-code-review技能调用）。未使用姓名；不涉及人工模型评分。

| 问题 | 修复与验证 |
|---|---|
| 超时后进程组首进程退出、孙进程仍存活 | 新入口独立实现PGID终止；真实CPU子孙进程测试先复现失败，再验证通过；旧冻结脚本不变 |
| 不同计划可同时通过瞬时GPU查询 | /tmp实例级flock加旧会话兼容锁，覆盖整个生成；嵌套争抢测试拒绝第二个计划 |
| 收费阶段未核对320停机 | 新评分计划绑定停机状态、观察证据和本次生成回执；错绑/仍运行/缺失拒绝，GPU主机禁止收费 |
| 环境只有记录没有约束 | 目标审计绑定完整freeze与核心包版本；生成重新校验Python、包版本与freeze哈希 |
| API仅socket超时、缺少总时长限制 | 评分在受监督独立进程中执行，7200秒墙钟上限，超时终止整个进程组，保留请求marker |
| CSV预期空列未核对 | 校验完整表头、全部单元格和空串，篡改拒绝 |
| 评分监督父进程收到SIGTERM会遗留worker | run_child统一捕获SIGTERM/SIGINT，finally清理完整组；清理期间防二次终止中断；真实CPU用例先红后绿 |

复审最终意见：此前报告的问题均有对应修复，无新增阻断。该意见只针对当前代码，不构成真实GPU评估或付费运行放行。最终测试结果见verification.json及原始日志。
