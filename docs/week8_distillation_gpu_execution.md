# Day42 GPU执行记录（2026-09-18）

本轮使用既定320实例（be044ebe99-be706b14）的RTX 3090，独立远端目录`/root/autodl-tmp/week8-distillation-gpu-20260918`。没有恢复旧推理会话/监控。

执行计划与脚本保存在`reports/week8/phase3_distillation_gpu/`。上传包及内部10个文件分别固定SHA256，远端解包前检查整包哈希，运行前再次校验内部文件。复跑相关37项回归通过，0失败、0错误、0跳过。

本轮只运行学生/教师CUDA冷加载及200条教师生成，不自动开始真实学生训练。单次冷加载最多300秒、教师生成最多1500秒；整个协调进程通过2200秒timeout包裹，子进程由既有run_bounded单独限制并清理进程组。平台定时关机为UTC 19:00（控制台显示20:00），内部截止UTC 18:55。完成后提前关机。

运行次序为文件身份复核、学生冷加载、教师冷加载、教师生成、答案质量审核。任何失败记录FAILED_NO_RETRY，不自动重跑。学生和教师CUDA BF16加载、有限logits及短生成已在本轮通过；教师数据质量仍需实测审核记录，不能仅凭生成程序完成放行训练。


## 最终执行结果

协调进程运行时间UTC 18:16:43–18:22:35，GPU为RTX 3090 24GB（UUID记录在本地launch回执中）。本轮核心回执及抽查包已经取回；控制台在18:28 UTC核对时显示320已关机，关机操作在此之前已提交，未继续占用GPU等候数据审核。

| 项目 | 实际结果 |
|---|---|
| 学生真实CUDA BF16加载 | 通过；有限logits，生成2 token；峰值分配显存1,260,388,352字节 |
| 第四周教师真实CUDA BF16加载 | 通过；有限logits，生成2 token；峰值分配显存15,272,736,256字节 |
| 教师生成 | 200条均生成，191条非空且未截断，9条截断已排除，空答案0条 |
| 导出时GPU任务进程 | 空 |
| 助手质量抽查 | 随机seed42抽取20条完整答案，补充2条边界指令，共22条 |
| 学生训练放行 | 不通过；尚未启动真实学生训练 |

191条只是程序按非空/未截断规则接受的数量，不代表191条质量合格。22条抽查结果为4条明确拒绝、1条源指令歧义、17条未见明显问题；该抽查并非全量正确性认证，不能直接将抽查错误率外推至全部数据。

### 已确认问题

1. 平均值算法：教师代码把`total`赋为元组，随后除以3，实际执行抛出`TypeError`。示例5、10、15的均值应为10，教师注释还错误写为7.5。
2. 长度约束：请求八个字，回答去除标点后有10个汉字，程序计数确认。
3. 人物事实：奥巴马介绍中出现错误的2008年民主党竞争对手姓名。同期报道确认主要竞争对手是希拉里·克林顿，见[当时的CBS/AP报道](https://www.cbsnews.com/news/obama-clinches-dem-nomination/)。
4. 目标语言约束：要求印地语，答案的类别标签混入英文片段，未完全满足要求。
5. 输入歧义：只有“她将要做这件事。”一句陈述，教师自行假定要求英译；归为源输入不明确，不作为确定事实错误计数。

每条sample_id、完整输入/回答及理由见`review_packet.json`与`quality_review.json`。质量回执状态为`REJECTED_PENDING_DATA_CURATION`，没有伪造训练放行记录，原始教师答案保持不变。

### 回收范围与下一步

本地已保存原始抽查包（解压内容SHA256验证通过）、5份核心回执的JSON副本、22条完整抽查输入/回答及逐项审核。`checks.json`复核了22条输入与原候选的绑定、两份CUDA回执和生成数量，并保存可执行代码/字符计数问题。

完整213文件归档保留在320独立目录下的`full_evidence.txt`，绑定SHA256=`c3e5db4e3f977add42a785277fa2a13e77bedf437582a9fc6c93c8b81574265c`。浏览器本轮未能直接导出全量文件；不声称本地已备份全部200条原始回答。压缩抽查包通过界面取回并校验完整哈希。后续应在无卡会话中回收完整归档，审核剩余169条，建立包含每条保留/排除理由的筛选记录，并将新目标文件绑定到原始生成回执。优先复用已有合格回答，不为少量坏样本重跑全部200条。

在数据审核和筛选记录通过、真实清洗后训练数量重新确定之前，不启动学生训练。两轮训练、合并权重验证及CEval/速度比较仍为后续工作；CUDA短生成通过不能代替这些验收。


## 2026-09-18 全量教师数据筛选续记

完整213文件现已回收到本地并逐个SHA256核对。200条均已逐项审核：152条保留、9条截断排除、39条完整回答因明确问题或核对不充分而排除；不把后者全部计作事实错误。原始回答和历史抽查记录保持不变。320无卡取证会话已关机，未启动学生训练。训练入口的可审计子集支持、真实学生tokenizer清洗与最终步数核定仍待完成。见[全量筛选记录](week8_distillation_curation.md)及`reports/week8/phase3_distillation_curation/`。
