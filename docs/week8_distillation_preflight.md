# Day42 蒸馏准备审核（更新至第六轮）

当前状态：第六轮真实CUDA加载及200条教师生成完成，320已关机。191条完整回答中的22条抽查发现4条明确错误及1条输入歧义，质量回执未放行学生训练。详见week8_distillation_gpu_execution.md；前五轮为历史记录。

## 已完成

1. 按需求明确教师为第四周最终纠偏DPO，不能改用第八周新DPO冒充。历史路径为/root/autodl-tmp/qwen25-week4/best_model/qwen25-7b-week4-dpo-corrective-merged；历史manifest指纹记录在phase3_distillation_preflight/plan.json。历史归档能说明来源，不能证明当前320上的权重仍存在或未变。
2. 学生保持Qwen/Qwen2.5-0.5B-Instruct，两轮训练。尚未固定下载revision或当前本地权重哈希，不称学生身份已通过。
3. 从正式1422条训练数据固定seed42抽取200条候选，逐条保留lineage样本ID、system、history、instruction和input，移除末轮output。历史上下文中的assistant文本仍属于输入上下文，不是本轮教师新目标。158条验证不用于抽样；当前精确提示交叉为0，不等同新一轮语义污染检查。
4. 修复distill.py与数据准备CLI的兼容性：原调用会被默认冻结协议拒绝；现调用已有清洗函数并显式绑定学生tokenizer、长度和seed。未修改被冻结的step1_data_prep.py。
5. 修复教师生成丢弃system/history的问题，学生目标也保留相同上下文。两项针对性测试先失败后通过；tokenizer使用离线替身，只验证数据流与划分，不是0.5B真实token长度或CUDA验收。

## 必须继续修复和核验

- 比较入口本地修复完成（第二轮见下文）；仍需在目标Linux环境确认0.5B真实tokenizer的提示长度、OpenCompass加载和CUDA前向。既有7B提示审核不能替代0.5B实测。
- 教师与学生当前文件身份必须在目标机核验。学生不仅检查hidden_size/layers，还要完整权重、tokenizer、模型revision和加载证明。
- 第三轮已补生成回执及训练前的数据抽查检查；实际教师目标尚未生成，质量抽查尚未进行。
- 第三轮已补两轮trainer_state和损失/梯度检查，移除仅凭导出文件宣布训练完成的状态。第四轮已接入LoRA/合并张量检查及独立冷加载；真实学生模型仍待目标机实测。
- 前后速度协议和子进程超时已补本地检查；CUDA计时、GPU UUID字段兼容性及目标机无其他负载仍待实测。比较入口的超时会终止其进程组，但不等于整机自动关机。
- 为新实验建立独立输出目录、截止时间和关机安排；不得恢复已完成的SFT/DPO推理会话或旧监控。

本轮200条为候选输入，不是教师目标、更不是已完成蒸馏。下一轮优先加固模型身份和CEval比较入口，然后目标环境核验，满足条件后开展教师生成和两轮学生训练。序列级教师文本SFT不等于软标签KL蒸馏，报告继续如实区分。

本地证据：reports/week8/phase3_distillation_preflight/plan.json、candidate_prompts.json、prepare_prompts.py、tests.log、verification.json。

## 第二轮：比较入口修复与本地验证（2026-09-18）

已修改`scripts/compare_distillation.py`，新增`tests/test_week8_distillation_comparison.py`：

1. 使用已冻结的数据和提示配置，只保留52科CEval、val划分、固定5-shot。模型和tokenizer都指向对应学生目录，不再使用简写`ceval_gen`或7B tokenizer作为实际评测tokenizer。旧配置构建器仍用于核验历史依赖，没有修改被冻结的源文件。
2. 使用冻结标准答案逐题计分，必须恰好52科1346题；拒绝缺科、重复科目、拼接不同运行目录、错误参考答案、错误正确性标记和伪造汇总分。`details.type`不计为题目。
3. 运行前要求两份模型manifest，校验绝对路径、文件集合、字节数、SHA256、权重分片索引，拒绝混入adapter或bin权重；前后tokenizer资产必须一致，每个模型评测后再次校验。此检查只能证明文件一致，不能证明模型来源、张量有效或训练成功。manifest放在模型目录外；模型目录应是无软链接的完整导出副本。
4. 速度测试要求2次预热、10次测量，每次实际生成128 token，时延必须为有限正数；保存GPU UUID、软件版本、题目来源和12个实际token输入哈希，前后不一致则不通过。包含prefill，尚无真实速度成绩。
5. 比较子进程共享默认7200秒执行预算，单次速度测试最多600秒；失败/超时终止对应进程组并记录失败。CPU文件校验不受硬时钟中断，因此该预算不能替代后续整机截止时间和定时关机。

测试使用真实历史SFT/DPO结果，独立复算为1072/1346和1077/1346。它们是验证新计分器的历史样本，不是0.5B蒸馏前后成绩。新增8项比较测试、既有2项蒸馏准备测试均通过；详细回归结果见`reports/week8/phase3_distillation_preflight/round2/tests.log`。

通过Codex内嵌浏览器只读查看320（be044ebe99-be706b14）：已关机、GPU充足、数据盘使用92.04%。本轮没有开机，没有读取到当前远程权重，也没有调用付费API。磁盘百分比不足以判断能否容纳新实验，需开新会话后读取实际剩余字节，预估学生权重、checkpoint和日志空间；不擅自删除旧模型。

下一步是完成教师生成回执绑定和训练完成校验，再建立独立320实验会话，核验Week4教师、学生revision/权重、实际可用空间及0.5B提示长度。旧推理会话和旧监控继续保持结束状态。无需新增姓名或人工评分手续。

## 第三轮：教师数据绑定与训练状态核验（2026-09-18）

`scripts/distill.py`已接入以下检查：

- 生成前核对第四周纠偏DPO历史manifest文件，再逐文件检查当前教师权重；生成后再次核对。生成使用的样本必须有唯一lineage sample_id。
- 保存选中的输入、教师身份、逐题原始回答和接受的训练目标；回执绑定全部文件SHA256、配置和源输入指纹。
- 训练入口必须收到generation_receipt和quality_review。逐条从原始回答重建训练目标，核对上下文、样本ID、答案及截断标记；文件缺失、变更、错配或抽查仍有未解决问题都会拒绝训练。
- 数据抽查记录绑定具体回执和目标文件，不要求姓名。至少抽查10条是本项目的工程质量检查规则，不是老师新增要求；由助手检查并记录理由，不追加一轮用户人工评分。当前没有真实教师答案，因此没有预填通过记录。
- 学生训练前检查模型文件清单，数据清洗后检查训练/验证数量和提示交叉；保存输入证据。训练退出后验证计划步数完成、逐步日志完整、损失/梯度/学习率有限、存在非零梯度与学习率，且两轮都有验证损失。
- 第三轮当时要求trainer_state报告epoch=2和num_train_epochs=2（第五轮已依据目标版本实测修正循环上限字段的解释，实际epoch仍严格要求2）。若目标版本的梯度累积导致提前结束或不同的epoch记法，先复核真实batch/step行为再调整，不能直接放宽条件掩盖少训练。
- 导出后明确写为`EXPORTED_PENDING_TENSOR_AND_COLD_LOAD_VERIFICATION`、`training_verified=false`；异常写FAILED。当前还不能自动升级为蒸馏成功，LoRA张量非零更新、合并张量和独立CUDA冷加载需后续完成。

### 第四周指纹口径核对

历史`manifest_sha256=aca9a293...`是按顺序拼接每个文件的“sha256 两空格 相对路径 换行”后计算的模型文件清单指纹，并非manifest JSON文件的SHA256。其原始算法见`deliverables/week4/day20/source/scripts/validate_merged_model.py`的build_manifest。

本地归档`deliverables/week4/day20/source/results/corrective_final/merged_model_manifest.json`文件SHA256为`39d6abfaa8d260be36524c10def770fd1381a6ad12d14f1cd145f220b39964a7`；模型文件清单指纹为`aca9a2932ed427f8fc6e9962e70274eb286a09c446d177af2bc00f6df1675e4c`。两者已分别核对。首轮plan中的historical_manifest_sha256保留历史字段含义，不改写旧证据。

新增4项证据检查测试，连同蒸馏准备/比较和评测/流水线回归共30项。结果见`reports/week8/phase3_distillation_preflight/round3/`。本轮没有启动GPU、生成真实教师回答或进行真实训练。

后续进入目标环境前仍需完成：有限时长的独立320会话及关机安排、可用磁盘字节与模型资产检查、学生revision固定、真实提示长度检查、生成及训练子进程的全程超时保护、权重张量和独立加载核验。上一轮控制台的GPU充足仅是当时观察，不能作为下一次开机时仍有空闲的保证。

## 第四轮：导出权重核验与独立加载入口（2026-09-18）

新增`scripts/week8_distillation_verify.py`，由训练入口在独立CPU子进程中执行：

1. 核对普通LoRA的rank=8、alpha=16及基座路径，拒绝未支持的DoRA、RSLoRA、额外保存模块或不同rank规则。
2. 同时支持单文件和分片safetensors；对照索引检查缺失/重复/额外张量，比较基座和导出模型的架构字段、张量名称及形状。
3. 检查所有基座、adapter、导出张量的有限性；未被LoRA修改的张量必须保持一致。
4. 按`W_export = W_base + 2 × B @ A`逐模块计算期望更新，在记录的浮点舍入容差内核对，并要求至少一个模块发生实际更新。BF16/FP16容差依赖对应dtype的epsilon和张量幅度，不声称逐位完全相等。
5. 保存导出目录的逐文件SHA256清单，作为后续比较入口所需的after-manifest。

`distill.py`随后调用既有`week8_full_cold_load.py`独立进程，使用本地导出模型执行CUDA BF16加载、有限logits检查及1–2个token生成，并校验回执。冷加载后重新核对模型文件清单；全部通过才写`STUDENT_TRAINING_VERIFIED_COMPARISON_PENDING`。这表示训练产物核验通过，不表示蒸馏质量更好，仍须完成CEval/速度对比。

产物核验子进程共享900秒预算，冷加载最多300秒。它们不替代生成/训练全程的会话截止时间和整机关机安排。

本地新增6项测试，使用真实PyTorch/safetensors小张量，覆盖正确合并、错误合并、未更新权重、NaN、缺失张量、额外非LoRA改动、分片缺失和BF16舍入。冷加载成功/失败的编排测试仅替换不可用的CUDA边界；合并检查worker实际作为子进程运行。不能把这些临时测试回执当作真实GPU证据。

隔离测试环境安装了PyTorch 2.14.0与safetensors 0.8.0，未改变项目或320的训练环境。相关回归总计36项，结果保存至`reports/week8/phase3_distillation_preflight/round4/`。本轮没有开机、生成真实教师回答或训练学生模型。

### 下一阶段目标机执行顺序

1. 建立独立320会话和截止时间，保留已结束的旧会话/监控；先检查磁盘实际可用字节、已有教师/学生目录和目标环境版本。禁止为腾空间直接删除未备份的模型。
2. 使用第四周已归档清单逐文件核验教师。学生固定来源revision和完整快照；检查tokenizer、架构、CEval提示长度及CUDA前向。
3. 在目标版本的小型训练检查中确认梯度累积、计划步数和epoch记录方式。上一轮历史7B训练的epoch曾为4.98734177而配置为5，故不得未核实就启动耗时实验，也不能把epoch浮点值简单视作充分的完成证明。
4. 在独立目录生成教师回答并审核，再训练学生两轮；执行本轮加入的张量/冷加载验证。
5. 使用同一学生tokenizer、固定52科1346题及速度协议完成前后对比，回收日志和证据，按新会话安排关机。

以上是实际执行顺序；尚无新的GPU运行窗口、远程权重身份或学生成绩回执。本地入口加固完成后，下一阶段应优先做目标环境核验，不继续增设人工评分手续。


## 第五轮：320无卡环境实测及两轮训练步数修正（2026-09-18）

本轮在独立目录`/root/autodl-tmp/week8-distillation-preflight-20260918`执行，使用0.5核/2GB无卡模式（标价0.10元/小时），不占GPU。新会话配置了18:30 UTC平台定时关机、18:25 UTC内部截止；18:07 UTC已从控制台确认提前关机。旧推理会话和旧监控未恢复。

### 目标环境实测结果

- 第四周纠偏DPO教师的14个文件（含4个权重分片）逐一核对字节数和SHA256，全部符合本地历史归档。教师身份通过，CUDA加载仍待后续。
- 学生固定为`Qwen/Qwen2.5-0.5B-Instruct`，revision=`7ae557604adf67be50417f59c2c2f167def9a775`。已下载8个必需文件；Hugging Face直连失败后使用ModelScope官方Qwen仓库下载，并以固定revision的官方Git blob/LFS哈希校验内容，不以浮动master作为身份依据。
- 权重988,097,824字节，SHA256=`fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe`。safetensors头部有290个张量、494,032,768个存储参数，hidden_size=896、24层；仅检查头部，未执行模型前向。
- 数据盘开始剩余21,363,585,024字节，下载后剩余20,363,460,608字节（约18.96 GiB）。未删除旧资产。新GPU会话仍须按实际checkpoint/导出数量核算空间并设置停止条件。
- 使用实际学生tokenizer与目标OpenCompass 0.5.3检查52科1,346题：全部5-shot，最长输入1,361 tokens，零截断，与独立冻结基线的输入token哈希全部一致。这是提示词构建检查，没有运行模型答题，也不是CEval成绩。

### 实测发现并修复的训练步数问题

目标Transformers 4.50.0在180条训练数据、batch=1、梯度累积8时，默认按向下取整计算总更新步数。实际CPU小模型试验结果：

| 设置 | 更新步数 | 实际epoch | 处理训练样本条次 | 验证epoch |
|---|---:|---:|---:|---|
| 仅配置num_train_epochs=2 | 44 | 1.9333333333 | 348 | 1、1.9333333333 |
| 显式max_steps=2×ceil(180/8) | 46 | 2.0 | 360 | 1、2 |

`scripts/distill.py`现在从真实清洗后的训练数量计算完整两轮所需更新步数，保存计划并限制使用已实测的Transformers版本。验收仍严格要求实际epoch=2、计划步数全部完成、每轮边界的验证记录、有限损失与非零梯度。此版本在显式46步时将`state.num_train_epochs`记为3，这是预先计算的循环上限；实际只处理两轮，不能用该字段单独判断训练了三轮。验收会依据计划核对该上限，不再固定要求它等于2。

CPU试验使用单参数小模型，只证明目标Trainer调度行为。真实LLaMA-Factory处理后的数据长度及学生训练仍需核验；如果真实日志不满足两轮条件，入口拒绝宣布成功。

### 证据回收与边界

证据目录：`reports/week8/phase3_distillation_target/`。7份远端JSON原始文本以压缩包形式从Jupyter界面回收，解包总SHA256及各文件SHA256均核对。`retrieved/`保存原始回执；`recovered_bundle.json`还记录5份仅保留在远端的脚本/提示配置哈希。远端脚本和完整prompt_rows尚未取回；本地`probe_epoch_schedule.py`是同逻辑参考脚本，不声称与远端执行文件字节一致。

本轮新增训练调度回归先失败后修复，相关37项测试通过（0失败、0错误、0跳过），见tests.log及test_result.json。CPU环境检查通过，不等于GPU执行已验收。

下一阶段：建立新的有截止时间和关机保护的320 GPU会话，核验学生真实CUDA加载/前向，核算空间并设置生成/训练全程超时；随后生成200条候选输入对应的教师回答，由助手审核目标质量，再开展学生两轮训练及前后CEval/速度测量。无需追加用户姓名或新一轮人工评分。


## 第六轮：CUDA加载、教师生成及质量审核（2026-09-18）

学生及第四周教师均通过真实CUDA BF16加载、有限logits与短生成；200条教师生成完成，9条截断排除，剩191条。抽查22条发现4条明确问题和1条源输入歧义，数据质量回执为REJECTED_PENDING_DATA_CURATION。代码报错与字数违规已程序复现。320提前关机；未训练学生、未生成学生CEval或速度成绩。

完整记录见`docs/week8_distillation_gpu_execution.md`，本地证据见`reports/week8/phase3_distillation_gpu/`。下一步转为无卡环境回收完整归档、审查剩余169条并形成可追溯筛选，再决定训练数据是否放行。无需新增用户评分手续。


## 2026-09-18 全量教师数据筛选续记

完整213文件现已回收到本地并逐个SHA256核对。200条均已逐项审核：152条保留、9条截断排除、39条完整回答因明确问题或核对不充分而排除；不把后者全部计作事实错误。原始回答和历史抽查记录保持不变。320无卡取证会话已关机，未启动学生训练。训练入口的可审计子集支持、真实学生tokenizer清洗与最终步数核定仍待完成。见[全量筛选记录](week8_distillation_curation.md)及`reports/week8/phase3_distillation_curation/`。
