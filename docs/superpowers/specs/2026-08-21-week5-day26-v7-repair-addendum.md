# Week5 Day26 v7 候选 D 预注册补充

开发集揭盲后发现原 `lora_wins >= 12/20` 与量表存在天花板冲突：Base 在 10/20 题已经达到 5.0，严格胜题的理论上限只有 10。该事实不改变已经生成的回答或评分，只说明胜题硬门槛在本题集上不可达。

候选 A、B、C 与 B 的 0.75/0.85/1.0 scale 比较中，B@1.0 唯一产生稳定正收益：均分 `+0.2425`，幻觉率 `45% -> 30%`；退化集中在自然场景和 UI，图表与公式明显改善。

在不解封 final 的前提下，预注册一个候选 D：

- 父 adapter 固定为候选 B，SHA-256 `759b1d4d856368a8b6b69c43f42ac95984a5714fa0e5bfca7e09af9599c41e2d`；
- 只重采样冻结的 200 条 train，绝不加入 dev/final 图片、提示或答案；
- natural_scene 与 UI 各重复 3 次，OCR 保留 1 次，共 280 条；
- 继续训练学习率 `5e-6`、1 epoch，其他 LoRA 与冻结配置不变；
- 仅在开发集以 scale 1.0 评测；不再搜索新的 scale；
- 若 D 未同时达到均分提升 `>= 0.35`、直接问不退化、幻觉率不升，则仍选择 B@1.0 作为工程最优，但不把 D 写成 PASS；
- 胜题数同时报告原始严格胜题与机会集胜率。机会集只包含 Base 分数低于 5.0 的题，避免把 Base 已满分题当成 LoRA 必须严格超越的题。

final 继续封存，直到候选和验收规则冻结。

## 候选 D 后的单点外推

候选 D 的开发集均分变化为 `-0.06`，因此拒绝该候选。B 的 scale 0.75、0.85、1.0 对应均分变化依次为 `-0.0975`、`-0.01`、`+0.2425`，呈单调上升。final 仍未解封时，预注册且只允许一个外推点 B@1.25；不测试其他新 scale。

B@1.25 只有同时满足开发集均分提升 `>= 0.35`、direct gain `>= 0`、幻觉率不高于 Base 才可进入最终冻结。严格胜题数继续原样报告，但由于 12/20 已证实超过理论上限，不再作为可执行门禁；改报 Base 非满分机会集中的胜率。若 B@1.25 不满足上述三项，停止开发集搜索。

## 搜索终止结论

B@1.25 的盲评分结果为：总体均分提升 `+0.27`、direct gain `-0.205`、严格胜题 `6/20`，幻觉率由 `50%` 降至 `35%`。它只通过幻觉率不升一项，未达到均分和 direct 门槛。因此按预注册规则停止开发集搜索，不解封 final，不更新教师提交包，也不把本轮标记为 PASS。当前工程最优仍是 B@1.0（均分 `+0.2425`、幻觉率 `45% -> 30%`），但同样不满足交付验收门槛。
