# 恢复训练数据收口与训练前准备 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task in the current session. Steps use checkbox (`- [ ]`) syntax for tracking. Do not dispatch subagents without an explicit user request.

**Goal:** 明确隔离本轮无法可靠使用的数据，生成可追溯的SFT候选子集及训练前审计材料，让恢复工作从反复改表进入数据收口。

**Architecture:** 原始工作簿、冻结包和历史模型只读；在单独的新目录生成候选数据、排除台账和审计结果。历史200条抽审与81条盲评分歧单独收口，不混入训练正文。当前计划不是训练执行结果或放行凭证。

**Tech Stack:** 仓库Python导入/发布模块、JSON/JSONL、SHA-256；本地文档与数据核验使用已配置运行时。真实token审计使用320上的实际Qwen tokenizer和LLaMA-Factory loader。

**Spec:** 用户在当前对话中要求说明下一步，并已确认本人主审及第二人独立复审；上游约束见[模型质量恢复计划](/Users/yifanren/Documents/商汤科技实习/.worktrees/pre-week7-model-recovery/docs/week2_week4_model_quality_recovery_plan.md)、[最新定点验收报告](/Users/yifanren/Documents/商汤科技实习/outputs/01a057f0-219e-7f43-9161-67e37e5d00d4/targeted_fix_audit_20260908/定点修正文件验收结论_20260908.md)。

## 本轮执行结果（2026-09-08）

用户追加授权：“那你先来完成前两步”。Task 1、Task 2已完成；Task 3–6未执行。生成3,885条候选、1,023条排除台账、4,908条处置和123份正文归档，56项相关回归测试及110项独立回读检查通过。状态为 `PROVISIONAL_NOT_RELEASED`，未启动AutoDL。为避免沿用旧的“尚未真人审核”状态，两份恢复文档仅增加了当前检查点，不因此勾选Task 4。

证据：[前两步完成说明](/Users/yifanren/Documents/商汤科技实习/outputs/01a057f0-219e-7f43-9161-67e37e5d00d4/recovery_closeout_20260908/前两步完成说明_20260908.md)。

## Global Constraints

- 本计划不得覆盖现有权重，不使用 344 机执行正式实验，也不得把已公开的 200 题继续当作独立终测。
- 从原始 Qwen2.5-7B-Instruct 开始，不从当前 Week 3 merged 继续叠加。
- 先建立新的开发集和隐藏终测；隐藏终测冻结后不参与 prompt、数据或超参数选择。
- 原制定方案轮次未授权执行；本轮用户新增授权仅覆盖Task 1、Task 2及其必要实现、核验和状态记录。不得扩展至Task 3–6、正式发布、远端启动或训练。
- 接受用户对本人主审和第二人独立复审的明确说明，不要求再次证明真人参与。不虚构第二审核人的姓名、签署时间或尚未作出的裁决。
- 原始Excel、冻结数据、历史评分和已完成实验结果不覆盖。所有后续状态变化有独立版本、来源ID及原因。
- 原有267项缺口暂退出本轮训练；需要补证不等于必须在本轮救回该样本。
- 人员确认不等于最终分歧裁决，也不自动覆盖确认后再次改写的正文。
- 不修改检测规则来让本轮267条通过。3条HTML误报先隔离，补丁修复作为独立后续工作。
- 320需要使用时沿用既有费用授权；每次不再使用后关机，保留关机核验结果。

## Material Passport

- Origin Skill: writing-plans; academic-research-suite / experiment-agent
- Origin Mode: plan
- Origin Date: 2026-09-08
- Verification Status: PARTIAL_EXECUTION_VERIFIED_TASK_1_2_ONLY
- Version Label: recovery_closeout_plan_v1_step12_executed

此状态仅表示前两步已经执行并核验，后续任务未完成。这里采用既有恢复实验目标，不重新开展研究问题征询。

## 本次范围和终点

第一批工作以“候选数据收口报告”为终点：实际纳入数、排除数、排除原因、来源映射、剩余技术问题都清楚。约3885条是去重和评测隔离前的候选数量，不预先承诺最终训练条数。

正式训练在最终数据审计、必要审核结论归档和评测协议冻结后进行。本文件列出进入训练的条件，但不伪造尚未形成的训练YAML、性能阈值或运行命令。

## 路径与文件职责

执行工作树：`/Users/yifanren/Documents/商汤科技实习/.worktrees/pre-week7-model-recovery`，沿用`fix/pre-week7-model-recovery`，不另建容器或切换344。

只读输入目录：`/Users/yifanren/Documents/人工审核包_20260905/outputs/01a079e2_定点修正_20260908`。

下列是整份计划的文件职责。前六项已生成；historical_review_summary、baseline_adjudication_status、corpus_audit、closeout_report属于后续任务，尚未生成。本轮另有step12_summary、独立核验收据及“前两步完成说明”，不冒充完整收口报告：

| 拟生成文件 | 用途 |
|---|---|
| `outputs/01a057f0-219e-7f43-9161-67e37e5d00d4/recovery_closeout_20260908/input_receipt.json` | 三份交回文件及冻结包摘要、输入版本、用户确认来源 |
| 同目录`dispositions_4908.jsonl` | 全部4908条的本轮处置，不丢弃排除记录 |
| 同目录`quarantine_1023.jsonl` | 初始838条HOLD与185条DROP的原因及原始来源 |
| 同目录`archived_revision_bodies.jsonl` | 本轮退出训练的123条已有修订正文；仅供追溯 |
| 同目录`provisional_sharegpt.jsonl` | 初始3885条候选，只有conversations训练字段 |
| 同目录`provisional_lineage.jsonl` | 原始ID、原始摘要、实际正文摘要、处置及修改来源 |
| 同目录`historical_review_summary.json` | 历史200条审阅、195条事实可评分、5条无法核验及分母说明 |
| 同目录`baseline_adjudication_status.json` | 81条实际最终结论的完成状态，不把建议自动变为裁决 |
| 同目录`corpus_audit.json` | 结构、来源、长度、拒答、任务覆盖和重复检查 |
| 同目录`closeout_report.md` | 第一批工作的实际收口结论及下一阶段入口 |

若执行时上述目录已存在，先读其收据核对状态；不覆盖，使用新版本目录。正式发布文件使用既有发布器的`reviewed_sharegpt.jsonl`和`reviewed_data_receipt.json`，与provisional文件区分。

## Task 1：固定输入版本和已确认的审核来源

**Files:** 读取新交回三表及冻结包；创建`input_receipt.json`。不修改提交的Excel或仓库验证器。

**Interfaces:** 输入为当前三表、冻结`workbook_payloads.json`和当前对话中的人员说明；输出为可追溯输入收据。

- [x] 用既有`import_review_workbook.py::import_rows(package, workbook, book_name)`重新核验01、02；原03/04继续作为独立盲评原表保留。
- [x] 重新计算三表与冻结包哈希，对照最新验收报告；输入变化时先明确差异，不能沿用旧的通过结论。
- [x] 记录用户已确认本人主审、第二人独立复审且未受干扰。人员参与不再列为“真实性未确认”。
- [x] 区分“用户确认的审核范围”“程序需要的格式字段”“尚未作出的内容裁决”。不凭空填写第二人的姓名，不把当前记录时间写成过去审核发生时间。
- [x] 对最终不变正文复用已有真实审核结果。只有正文后来改变或存在具体未决内容时，才做定点核对，不要求全表重复审核。

验收：输入版本唯一、原表未变、人员说明有来源；后续程序若还缺格式字段，精确列出最小差额，不重新质疑人工参与。

## Task 2：将267项缺口明确隔离，生成候选子集

**Files:** 读取02、`02_待补材料及未关闭事项.json`、`02_修订正文.jsonl`和冻结`sft_review_inputs.jsonl`；创建处置、隔离、正文归档、候选及来源文件。

**Interfaces:** 按sample_id和原始content_sha256连接，不用Excel行号作为唯一键。读取历史核查明细时必须使用current_decision，不能将2435条历史实体都当成当前REVISE。

- [x] 核对待隔离267项确为当前REVISE：144无完整正文，123有正文但仍有缺口。
- [x] 将这267条在新处置文件中改为HOLD。已有123份正文复制到独立归档；活动处置中的revised_conversations清空，原因保留。
- [x] 原KEEP1720、原HOLD571、原DROP185不因本轮分流自动改变；剩余REVISE2165保留当前已核验版本。
- [x] 生成候选训练正文：KEEP取冻结原文，REVISE取最终选用正文，不取审核备注、证据栏、FullText展示分片或历史修订归档。
- [x] 独立按ID集合复算纳入与排除关系，检查没有缺行、重复ID、交集或未知ID。

应成立的初始验收式：

```python
assert 1720 + 2165 + 838 + 185 == 4908
assert 144 + 92 + 18 + 3 + 10 == 267
assert 1720 + 2165 == 3885
assert 838 + 185 == 1023
assert included_ids.isdisjoint(excluded_ids)
assert included_ids | excluded_ids == all_4908_ids
assert all(row['revised_conversations'] is None
           for row in dispositions if row['decision'] in {'HOLD', 'DROP'})
```

以上关系已由本轮构建和独立回读程序实际核验通过。若后续发现额外质量或评测重叠问题，应另增排除记录并更新数量，不能为了维持3885而放行。

## Task 3：完成历史数据问题的结论，处理真实分歧

**Files:** 读取01、原始03/04、当前裁决表；创建历史摘要及裁决状态文件。原表保持不变。

**Interfaces:** 历史抽审的TRAIN编号不等于候选数据行号；需要跨集合定位时用来源、sample_id和内容摘要连接。

- [ ] 将5项历史事实状态明确记为“无法核验”：TRAIN-0024、0038、0055、0078、0107。分数保留缺失，不填0或1，不当作正确或错误。
- [ ] 历史事实统计报告195/200有效评分和5/200缺失；保留全部200条样本，不用195条的均分冒充完整200条事实均分，不混同其他维度覆盖率。
- [ ] 检查这5项中的同一无据答案是否仍在本轮候选中；不能仅凭抽审编号机械删除，也不能把只做文本抽取的样本误当作对其中事实作真实性背书。
- [ ] 处理81条分歧：已有实际裁决则导入；没有实际裁决则保留建议、列出需要选定的最终值和理由。只处理分歧，不重做400条。
- [ ] 形成最终主表后，重新与原独立复审表比较，再生成对应当前分歧的机器裁决记录；保留原主表和复审表。

验收：缺证据有明确结论、统计分母诚实；裁决状态如实记录。该支线不阻塞Task 2和本地技术审计，但不能把未完成的最终评分当成已完成的模型质量结论。正式训练前按既定恢复方案核对必要前置条件，不能悄悄跳过。

## Task 4：检查候选是否适合训练，而非只检查能否读取

**Files:** 读取候选正文与来源；创建`corpus_audit.json`及`closeout_report.md`。

**Interfaces:** 所有检查绑定实际候选字节与摘要，输出问题ID及明确原因，不修改冻结原文。

- [ ] 逐条检查human/gpt角色交替、空消息、全部助手轮次、原始来源和最终正文摘要。
- [ ] 对全部候选运行现有`review_release.scan_all_turns`；凭据、控制字符、未闭合代码围栏和真正占位内容不得进入训练。不关闭扫描器来消除误报。
- [ ] 检查完全重复、同题近重复、同源模板簇和前后矛盾回答；近重复候选需内容判断，不用单一相似度分数自动认定完全重复。
- [ ] 统计语言、任务类型、单/多轮、长度、代码比例和拒答/澄清比例，检查清洗是否使数据只剩短回复或“无法回答”。
- [ ] 同一问题簇用于划分时保持在同一集合；发现额外问题就隔离并重新汇总，不为保持预期条数降低标准。
- [ ] 汇总第一批的实际结果：纳入数、排除数、无法评分数、裁决完成数、剩余技术问题；不以文件生成成功代替验收。
- [ ] 将实际收口状态及用户已确认的人员参与同步到恢复工作树的`deliverables/model_recovery/README.md`和`docs/week2_week4_model_quality_recovery_plan.md`。旧日期的审计结论保留历史语境，新增当前检查点；不能继续把9月5日“尚未人工审阅”的旧状态写成当前事实，也不能提前写模型恢复成功。

验收：候选集合无未处置的确定性阻断，来源与处置闭合，任务覆盖及可能的偏置清楚。此时可形成数据候选，不表示正式训练已经获准。

## Task 5：正式发布与评测隔离准备

**Files:** 使用已有`build_reviewed_sft_dataset.py`、`review_release.py`；发布到新版本目录，输出既定两份发布文件。另创建最终数据及评测协议收据。

- [ ] 将用户已经确认的实际审核结果按真实范围整理为发布器所需结构；不将程序要求的字段伪造成额外发生过的审核事件。
- [ ] 区分来源摘要、修订正文摘要和绑定声明摘要；使用最终处置及实际选用正文重新计算，原始确认文件仍留存。
- [ ] 使用现有发布校验，保留失败信息；缺失或范围不匹配的记录先准确定位，不能批量将revision_verified改为TRUE来绕过。
- [ ] 生成新的开发集和最终测试集，明确训练、选模、最后验收三种用途。已公开旧200题仅作历史诊断。
- [ ] 在训练前固定任务类别、评价指标、各类别底线、非劣效容差、候选预算和样本数量依据；不根据看到的测试成绩调低标准。
- [ ] 检查训练与评测之间的精确/近重复，冻结集合及评分代码摘要。最终测试内容不交给后续调参过程使用。

发布器真实接口：

```text
build_reviewed_sft_dataset.py
  --decisions       最终4908条处置JSONL
  --attestations    与实际最终处置绑定的真实确认记录JSON
  --revision-attestations  实际修订核验记录JSON
  --output-dir     不存在的新发布目录
```

此处是接口说明，不是可提前运行的训练命令。发布器的成功状态仍为`REVIEWED_DATA_ONLY_NOT_TRAINING_READY`。

## Task 6：320上核验真实训练输入，之后才冒烟训练

**Files:** 最终数据、来源、实际训练配置、token审计与环境收据；所有运行产物位于320上独立新运行目录，回收至本地对应目录。

- [ ] 仅启动320（实例`be044ebe99-be706b14`），核实实例身份、GPU、剩余磁盘和现有GPU进程；不在344继续正式实验。
- [ ] 核验原始Qwen2.5-7B-Instruct模型文件、代码版本和实际依赖；本地/远端数据哈希一致才继续。
- [ ] 针对最终ShareGPT集合构造真实loader审计。已有`audit_remote_training_inputs.py`用于历史SFT/DPO重建，不能直接拿历史通过结果证明新数据通过；若需要新入口，先做范围明确的实现及测试，再运行。
- [ ] 逐条核对input_ids、attention、labels、EOS、padding及预期助手监督区间；用户问题不能被错误计入loss，多轮早期助手答案不能因格式错误遗漏。审计所有最终行，不仅抽几条展示。
- [ ] 根据实际长度分布选择cutoff，检查超长样本完整性；不得沿用历史“无截断”结论认定修订后的正文也无截断。
- [ ] 数据及评测准备通过后，将保守训练配置写成实际YAML并锁定摘要。该配置才成为后续运行命令依据；当前不猜测最终参数。
- [ ] 先做独立工程冒烟：建议覆盖长/短、单/多轮、代码等约64条样本，约20个优化步骤，并至少验证一次保存与恢复；实际预算在运行前固定。冒烟只验证训练链路，不用于证明质量提升。
- [ ] 检查loss/梯度是否有限、监督token是否非零、目标参数是否更新、显存和检查点是否正常。不静默重跑失败任务，不将失败目录覆盖。
- [ ] 每次不再使用320后关机并核验；无GPU时继续本地工作，不新建替代实例。

验收：真实训练输入正确，训练/保存/恢复链路可运行。错误消失和loss下降不是模型能力提升的证明。

## 后续受控实验的边界

- 假设：修复数据与监督方式后，从原始基座重新SFT可以在保持关键能力的前提下改善目标任务。该假设尚未验证。
- 对照：原始Qwen2.5-7B-Instruct，使用相同推理设置及同一评测协议；旧退化模型作为历史参考，不作为新模型起点。
- 先执行少量保守候选，保持非目标参数一致；每个正式候选沿用恢复方案的至少3个seed，保留全部失败运行、均值和离散度。
- 用开发集选定唯一候选，再一次性使用冻结终测对照基座；不能反复看终测选最好的模型。
- 总体、代码、安全和关键业务能力达到预先固定条件后才进入DPO；不以训练loss、一个seed或某个局部题目替代质量验收。
- SFT未通过就停在SFT诊断阶段；DPO及Week7不自动开始，不能把未通过模型包装为正式成果。

## 执行交接

本轮交付的是下一步方案，不是上述任务已完成的声明。建议用户确认开始后，在当前任务内按Task 1—4完成第一批收口，展示实际数据取舍结果，再进入发布、评测隔离和320真实输入审计；不另建任务、不默认分派子代理。

## 自检

- 输入、输出和验收条件均已明确，所有拟生成文件与现有文件区分。
- 267条隔离后的初始处置总数为4908，HOLD/DROP的活动修订字段必须清空，正文另存追溯归档。
- 人员参与、实际内容裁决和程序格式要求分开处理，不再次要求证明真人身份。
- 不通过猜分补齐5项事实缺失，不把81条建议等同最终裁决。
- 保留320唯一实例、原始基座起训、三seed、开发/隐藏终测隔离和不覆盖历史的约束。
- 没有创建训练数据、更新审核结论、修复代码、启动GPU或执行训练。
