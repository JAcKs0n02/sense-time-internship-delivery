# Week8 发布与模型备份审查

> 2026-09-19验收口径更正：老师要求干净环境至少运行数据准备与评估，并支持分段执行；没有要求以Linux CUDA重跑训练、蒸馏、部署全流程作为本项验收。下文历史“完整CUDA复现”待办由[当前准备方案](week8_clean_eval_preparation.md)取代；历史实验回执和冻结候选不改写。

审查日期：2026-09-19（Europe/London）。结论：本轮审查完成，发布与删除中间检查点均不放行。只检查本地文件与已有远端证据，没有启动实例、调用付费API、修改Git历史、提交或推送。

## 发现及处理

| 优先级 | 发现 | 本轮处理与剩余动作 |
|---|---|---|
| 高 | 本周最终SFT、DPO及蒸馏模型未发现本地完整副本 | 从原始merge回执提取逐文件SHA与大小，预期合计31,498,338,719字节，约31.50GB；备份证明仍缺失，禁止清理源模型或检查点 |
| 高 | 历史数据9个已跟踪文件含疑似凭据字面量 | 仅保存路径、行号与命中数量；有效性未知，未测试。发布副本应脱敏并记录转换关系，原始实验数据及原始哈希保持不变；已有Git历史需要单独审查，不能靠.gitignore解决 |
| 中 | 初始9078个Git已跟踪/未忽略文件共约1.28GB | 发现临时输出、重复tokenizer、视频及渲染副本；本轮只忽略确定为独立交付的Week7资产、报告渲染与旧版备份。其余outputs和证据仍需按运行依赖制定发布白名单，不可直接git add . |
| 中 | 提交校验表1项不匹配、16个文件未覆盖 | 备份旧清单后只修正Submission/README.md哈希；原439项现在全部匹配。本轮额外生成完整Submission清单，不改写历史实验证据 |
| 中 | 干净CUDA完整流水线一次运行缺少证据 | 本轮quick和score-only通过，仅代表历史回放与已完成评分的离线核验，不能代替CUDA全流程 |
| 低 | models/README仍称蒸馏未训练，模型角色过时 | 已更正历史模型与本周新模型的角色、位置和备份状态 |

疑似凭据来自第2周ShareGPT样本内的代码文本，在原始、格式化、清洗和提交副本间重复；不能据此认定是本次配置的裁判Key，也不能认定它仍有效。首次宽松模式还命中了Top200PerTask文件名，已用边界匹配排除。没有在审查结果中保存凭据原文。此扫描仅覆盖当前工作区Git跟踪及未忽略的文本文件（不超过20MiB），未覆盖完整Git历史、归档内部或二进制文档内容；它不构成全仓库无敏感信息证明。

## 本轮核验

- Week7资产清单16个文件重新读取并计算SHA-256，全部匹配；包括AWQ权重、配置、tokenizer和四段录屏。仅验证文件完整性，没有重新加载模型。
- 7个主要入口文档的72个本地链接存在；新增模型说明和审查文档另行复核。
- `PIPELINE_PYTHON=/tmp/week8-score-entry-clean-20260918/bin/python bash run_pipeline.sh --score-only all --run-dir logs/release-review-20260919-score`成功，结果为VERIFIED_EXISTING_SCORES，零新增API调用。
- 同一独立Python环境运行`--quick --run-dir logs/release-review-20260919-quick`成功，14行历史成绩回放，fresh_model_inference=false。运行基于当前完整工作区，尚不是发布白名单导出后的干净clone验证。
- Submission旧校验表439项复核通过；旧表原件和本轮完整清单分别保留。

## 后续执行顺序

1. 按`expected_model_backups.json`核查320远端源文件和备份目的地可用空间，复制三个最终模型的完整目录；逐文件SHA、分片索引完整性与备份独立加载通过后，才能提出具体的检查点清理名单。远端是否仍存在、是否另有备份，本轮未重新检查。
2. 建立独立发布副本和明确文件白名单，对疑似凭据进行脱敏，保存变更映射和新校验表；重跑受影响的数据入口验证，说明脱敏副本与历史训练输入的区别。若处理已发布历史，需先明确仓库历史范围与协作影响，不直接重写历史。
3. 在发布副本执行安装、离线入口和依赖检查；补齐CUDA一次完整流程的验收证据，再进行最终发布审查。

本轮证据：[审查目录](../reports/week8/release_audit_20260919/verification.json)。模型目录中的配置文件与训练回执不能代替权重备份；远端路径存在于文档也不能证明远端目前可用。

## 2026-09-19 后续：模型备份缺口已关闭

上述内容保留为备份前的审查快照。后续已通过320无卡模式取回三个最终模型的38个文件，共31,498,338,719字节；下载哈希与独立全量复核均通过，分片索引及339/339/290个张量对应正确，三个模型在本机离线新进程中加载和短文本生成通过。320已确认关机，未删除任何远端文件。见[备份验收](week8_model_backup.md)。

这只关闭“本周最终模型缺少独立完整备份”一项。发布副本脱敏与白名单、干净环境及完整CUDA流程验证仍待完成，发布与未指定范围的清理仍不放行。

## 2026-09-19 后续：独立候选与CPU验证已完成

已经导出本地候选、明确5922项源白名单、处理9个JSONL文件中的18处已知凭据字面量并记录前后哈希；原始数据未变。三个离线入口、正式准备15文件字节一致性和全部170项测试均通过。未修改历史哈希门槛，失败尝试及修复经过均有记录。详见[候选验收](week8_release_candidate.md)。当前尚不允许最终发布：完整CUDA复现、历史公开范围及候选附件范围仍需最后审查；本轮没有Git提交或推送。
