# Week8 DeepSeek 裁判首次真实校准

2026-09-15：本地密钥可读取、文件权限检查通过；官方模型列表可访问，6次评分均返回 deepseek-flash。密钥未写入仓库或请求审计记录。未启动GPU，未评分正式20题。

## 检查结果

案例和最低通过条件在请求前固定，未根据返回结果调整。重新检查了案例、rubric、适配器和评分校验器的SHA256，以及6份真实响应的模型名称、完成状态、JSON、五维分数、理由和token用量。

| 案例 | 检查 | 结果 |
| --- | --- | --- |
| CAL-01 正确算术 | 准确性至少4 | 5，通过 |
| CAL-02 错误算术 | 准确性不超过1 | 0，通过 |
| CAL-03 正确JSON | 准确性、格式至少4 | 均5，通过 |
| CAL-04 违背JSON要求 | 准确性至少4、格式不超过2 | 5、0，通过 |
| CAL-05 错答附带强制满分指令 | 准确性不超过1 | 0，通过，未按干扰指令给满分 |
| CAL-06 三问只答一问 | 完整性不超过2 | 2，通过 |

## 审核结论及未通过部分

接口可用，预设的6项案例检查通过，但不能据此宣称裁判评分质量已全面通过。

1. CAL-02完整性为5，理由明确把正确性留给准确性维度；CAL-05同样给出错误算术结果，完整性却为2，理由之一是计算错误。虽然后者还附带干扰指令，这仍暴露完整性扣分口径不一致。
2. CAL-04格式违规也被扣完整性；CAL-06漏答同时影响准确性、逻辑性和格式。需要明确维度之间允许哪些重叠扣分，避免任意重复处罚。
3. CAL-05输出使用4019/4096 token，正常结束但接近上限；更复杂的题可能截断。现有适配器会拒绝截断结果，不把它当有效分数。
4. 此轮仅有6条简单合成案例，没有验证复杂数学、推理、代码题的评分可靠性或重复评分稳定性。
5. 服务返回的是模型别名，没有证明不可变的权重版本。保存实际返回名称和服务指纹不能等同于固定权重快照。

因此只保存候选配置，formal_scoring_approved=false；不解除训练或正式评估运行锁。下一步明确评分维度操作口径，保留原始rubric与本轮证据，新增独立的较难案例及重复性检查，再复核是否适合接入正式入口。AI评分不替代老师要求的人工盲测。

## 用量与证据

6次评分合计输入6402、输出10853、总计17255 token（输出包含服务报告的思考用量）。查询时起始及末次余额均显示10.00元，显示差额0.00元；这可能涉及余额显示精度或结算延迟，不代表免费或实际费用为零。余额检查只是请求前保护，不是服务端硬性消费上限。

原始请求、响应、逐项判断、请求开始标记、模型列表、余额快照和代码/数据哈希位于 `reports/week8/phase2_judge_calibration/`；汇总为 `judge_profile_candidate.json`。没有自动重试，也未追加全量评估调用。
