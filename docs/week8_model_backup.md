# Week8 最终模型独立备份

日期：2026-09-19（Europe/London）。结论：三个最终模型独立备份验收通过，320 已确认关机；本次未执行检查点清理或仓库发布。

## 范围与验收条件

本次从 AutoDL 320（`be044ebe99-be706b14`）读取三个最终模型的完整目录，保存到本机 `models/backups/week8-20260919/`。使用无卡模式，不重新训练、不调用裁判 API。

| 模型 | 本地子目录 | 文件数 | 预期字节数 |
|---|---|---:|---:|
| Week8 最终 SFT | `week8_final_sft` | 14 | 15,247,180,172 |
| Week8 最终 DPO | `week8_final_dpo` | 14 | 15,247,180,172 |
| Week8 蒸馏学生 | `week8_final_distilled` | 10 | 1,003,978,375 |
| 合计 | | 38 | 31,498,338,719 |

核验基准来自原始合并回执及蒸馏模型清单，见[预期文件清单](../reports/week8/release_audit_20260919/expected_model_backups.json)。该文件保留原审查时的状态；后续成功回执另行保存，不改写历史证据。

以下条件均已通过：

1. 远端目录的预期文件名称、大小完整匹配。
2. 下载后的全部文件逐个计算 SHA-256，与原始回执匹配；未完成文件保留 `.partial` 后缀。
3. 独立进程重新读取全部文件、复核哈希，检查 safetensors 数据偏移、张量名和分片索引对应关系。
4. 本机离线环境分别以新进程加载三个模型和 tokenizer，检查前向输出有限、短文本生成可以执行。
5. 验证 320 已关机，形成带哈希的完整验收回执。

## 传输处理

首次整文件请求提前结束，未通过文件大小检查，失败信息保留为 `first_full_response_attempt_error.json`。确认服务支持 HTTP Range 后改为每次 8 MiB 分段读取，严格检查 206 响应、Content-Range 和实际长度；保留已下载内容续传，最终仍要求完整文件哈希匹配。服务对小文件的完整范围请求返回 200，因此仅在起点为 0、终点等于预期文件末尾时接受 200，仍核对完整长度和哈希；实际探测记录保存在 `http_compatibility_check.json`。并发先从 4 路调整为 12 路，剩余六个大文件阶段改为最多 6 个文件各读取 2 个分段，总请求并发上限仍为 12，按连续偏移顺序写入。配置与进度采样单独留档。

CPU 独立加载使用临时隔离环境：Python 3.11、PyTorch 2.6.0、Transformers 4.50.0、Accelerate 1.5.2、safetensors 0.5.3；启用离线模式和 `local_files_only=True`，关闭远程代码信任。每个模型生成最多 8 个新 token。这只证明本地副本可加载和执行，不是新一轮能力评测，也不改变原有模型晋级结论。

加载日志原样保留了 Transformers 的提示：确定性生成会忽略原配置中的 temperature、top_p、top_k；该版本 Qwen2 的滑窗警告仅检查 sliding_window 值，而三个模型配置均为 use_sliding_window=false。本次提示长度远低于滑窗长度，验收范围仅限短文本加载与生成，不据此宣称长上下文行为已验证。

证据目录：`reports/week8/model_backup_20260919/`。其中可复用下载脚本从环境变量读取 Jupyter token，仓库脚本不内嵌凭据；权重目录已被现有 Git 忽略规则排除。

## 实际验收结果

| 检查 | 结果 |
|---|---|
| 原始回执与预期文件身份 | 38/38 文件的 SHA-256 与字节数一致 |
| 远端目录与本地完整文件 | SFT 14、DPO 14、学生 10；无遗留 partial 文件 |
| 下载时核验、独立进程重新核验 | 两次全量哈希通过，共 31,498,338,719 字节 |
| safetensors 与分片索引 | SFT 339、DPO 339、学生 290 个张量；无重复张量名，索引对应一致 |
| SFT 独立 CPU 加载 | 7,615,616,512 参数；有限前向值；短文本生成通过 |
| DPO 独立 CPU 加载 | 7,615,616,512 参数；有限前向值；短文本生成通过 |
| 学生独立 CPU 加载 | 494,032,768 参数；有限前向值；短文本生成通过 |
| 320 收尾 | 内嵌浏览器控制台明确显示“已关机”；未释放实例、未删除源文件 |

三次短文本检查均对固定提示生成了“1+1等于2。”，只用作运行检查，不能外推模型在数学、代码或长上下文任务上的表现。独立全文件复核完成于 2026-09-19 00:15:03 UTC（伦敦当地 01:15:03）；关机确认时间见回执。无新增 GPU 任务或裁判 API 调用，无卡运行账单未单独结算核对，保留的数据盘仍按平台规则计费。

主要证据：[总验收回执](../reports/week8/model_backup_20260919/verification.json)、[逐文件清单](../reports/week8/model_backup_20260919/backup_files_manifest.json)、[独立哈希与索引复核](../reports/week8/model_backup_20260919/independent_file_verification.json)、[关机确认](../reports/week8/model_backup_20260919/instance_shutdown.json)。三份 CPU 加载 JSON 与原始日志、环境版本清单位于同一证据目录。

## 后续边界

备份完成后仍需单独审查具体检查点清理名单，不自动删除任何远端模型或历史实验文件。本次备份也不代替发布副本脱敏、文件白名单、干净环境复现及完整 CUDA 流水线验收，整体发布状态仍以[发布审查](week8_release_review.md)为准。
