# Week8 正式训练目标机执行记录

2026-09-16：完整训练、两次合并及独立加载已完成，本地证据复核通过；320机已关机，监控暂停。正式质量评测尚未开展。

## 启动前检查

- 仅使用内嵌浏览器及既有Jupyter登录，目标为320机 be044ebe99-be706b14。
- 页面GPU充足，单价¥1.48/小时。平台关机保护已保存为页面时间2026-09-16 22:45，即21:45 UTC（北京时间9月17日05:45）。入口提前180秒停止，保护窗口约4小时，页面预计¥5.85；结束后应提前关机。
- 根据历史epoch-e5实际6245步/19137秒，1775步按比例约1.51小时；仅作为粗估，另留验证、预处理、两次CPU合并和GPU加载余量。并未降低训练参数来适应费用。
- 空闲盘约49.63GiB，高于45GiB门槛；内存限额60GiB；CUDA BF16可用。GPU启动前无其他任务。
- 原始模型15文件、8项依赖版本、40个部署输入均通过校验。部署到全新独立runtime，不覆盖旧smoke或历史模型。

## 当前运行

入口启动：2026-09-16 17:51:43 UTC。coordinator PID 1526。

目标目录：`/root/autodl-tmp/week8-full-training-runtime-20260916`。
运行输出：`logs/full-run-01`；主日志：`coordinator.log`；启动记录：`launch.json`。

17:52:53 UTC回执确认：SFT训练1422条、5轮、总1775步，已到第16步；所回收初期loss/grad/学习率均有限，GPU利用率100%，显存8428MiB。这仅证明完整配置成功启动，不代表训练完成或质量提升。

## 本地证据

- `reports/week8/phase2_full_training_target/release.json`：本次实际执行许可，4小时保护值及具体关机时间。与候选相比仅启用执行、绑定当前会话并调整运行保护窗口，训练配置不变。
- `deployment.json`：部署包、release和启动回执SHA256，以及当前状态。
- `formal_start_receipt.json`：从目标机下载的原始启动记录、预检、训练头部及日志尾部；下载后与目标机SHA256核对一致。
- `week8-formal-training-20260916.tar.gz`：40个冻结输入及实际release，共41文件，包内哈希逐项验证。

## 持续审核与结束条件

原week8-320任务已更新为正式训练监控，ACTIVE，每5分钟检查一次；禁止重新运行三步测试、重复启动或覆盖当前结果。无实质变化时安静等待，阶段完成、异常或需要用户操作时通知。

SFT完整验收、合并和独立加载均通过后，入口才会启动DPO40步。任何失败停止，保留证据，不改变超参数自动重跑。完成或失败后下载小体积原始证据并详细复核，及时关机、确认已关机、清除残留关机计划并暂停监控。大型模型权重留在目标机。正式质量评测和裁判API调用不在本次入口内。

## 19:00 UTC 阶段更新

SFT以退出码0结束，目标机验收确认1775步、392个adapter张量、196个非零LoRA B张量、优化器1775步；CPU合并已退出并进入合并验收，独立加载和DPO尚待确认。五次验证loss为0.9285、0.9941、1.1665、1.3968、1.5658，存在过拟合迹象，不能据链路通过声称效果提升。原始完整证据仍待最终回收复核。

## 19:07 UTC 阶段更新

SFT合并339个张量核验通过，独立CUDA BF16加载及有限logits/2-token生成通过。DPO已启动，训练头确认783条/40步，观察到23步及20步验证loss 0.688305；剩余DPO、合并及独立加载继续由门禁控制。证据尚待最终原始文件回收复核。

## 最终验收与关闭

19:13:39 UTC最后的DPO独立加载检查退出0，完整流水线结束。19:17 UTC左右确认320机已关机，残留定时关机已清除，week8-320监控PAUSED。实际从约17:49 UTC开机到关机约1小时28分；费用以平台账单为准，未运行到4小时保护上限。

已回收90份原始证据文件，压缩包19,567,978字节，包及每个文件哈希一致。40份冻结输入重新核验。`review_evidence.py`可重复执行本地复核，输出`target_review.json`，结论为FORMAL_TRAINING_PASS_LOCAL_REVIEWED。

| 检查项 | SFT | DPO |
| --- | --- | --- |
| 完整优化步数 | 1775 | 40 |
| adapter张量/非零LoRA B张量 | 392 / 196 | 392 / 196 |
| 优化器步数（目标机张量验收） | 1775 | 40 |
| 合并权重张量/分片 | 339 / 4 | 339 / 4 |
| 独立加载 | CUDA BF16、有限logits、2-token生成通过 | CUDA BF16、有限logits、2-token生成通过 |

10个子进程回执退出码均为0且未超时；原始日志哈希、实际候选参数、模型来源、完整Trainer步数和验证记录与验收回执相符。DPO启动时间晚于SFT独立加载结束时间。二进制adapter、优化器及合并大权重仍在目标机，本地没有重新执行张量验收；本地复核的是回收原始日志、配置、状态及目标机验收回执。

SFT五次验证loss：0.9285、0.9941、1.1665、1.3968、1.5658；其持续上升提示过拟合，不能据训练成功声称效果提升。DPO20/40步验证loss为0.6883/0.6704，末次偏好准确率约0.7126；它是内部偏好验证指标，不等于最终任务质量分数。

最终模型位于目标runtime的`logs/full-run-01/models/final_sft`与`final_dpo`，各文件身份已记录于对应merge_verification.json。下一阶段应为这两个新模型建立独立评估锁，再开展原定CEval、CMMLU及custom20，与原基座比较。旧original_base锁不可直接套用。本次未调用裁判API。
