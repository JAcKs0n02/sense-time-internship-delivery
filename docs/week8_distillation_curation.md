# Day42 教师答案全量回收与筛选（2026-09-18）

本轮已完成原始证据回收、200条逐项阅读和筛选。当前状态：`CURATION_COMPLETE_TRAINING_GATE_PENDING`。保留152条原样教师回答，尚未启动学生训练，也没有签发训练放行回执。

## 实际结果

| 项目 | 数量/结果 |
|---|---:|
| 回收到本地并逐文件核对SHA256 | 213个文件 |
| 生成回执内部文件绑定核对 | 203个文件 |
| 原始教师输出及完整对话上下文逐项审核 | 200条 |
| 完整、非空输出 | 191条 |
| 截断排除 | 9条 |
| 完整回答中排除 | 39条 |
| 保留，未改写 | 152条 |
| 与原158条验证集的完整提示交集 | 0 |

所有200个sample_id均通过lineage映射到原1422条训练集的对应行；指令、输入、系统消息、历史对话逐项一致。候选200条的seed42顺序与远端selected_prompts完全相同。原191条目标可从200份原始回答精确重建。

39条排除包含明确错误、指令约束未满足、缺失上下文，以及无法充分核对的标签/翻译/来源。**不能把39/191当作客观错误率**：保守排除并不等于证明答案错误。本轮由助手阅读审核，不是新增人工评分、独立裁判评分或全面外部事实认证，不需要姓名信息。

## 可追溯文件

均位于`reports/week8/phase3_distillation_curation/`：

- `full_bundle.json`：完整证据包；SHA256为`c3e5db4e3f977add42a785277fa2a13e77bedf437582a9fc6c93c8b81574265c`。
- `retrieved/`：213份原始文件；`generation/teacher_raw/0000.json`至`0199.json`保留全部教师输出。
- `curation_manifest.json`：200条决定、sample_id、原始文件哈希和逐条理由，并绑定生成回执和筛选后文件。
- `curated_targets.json`：152条保留项，保持原输出内容与原相对顺序，不以助手改写冒充教师生成。
- `checks.json`：来源、数量、哈希、排除覆盖、可执行样例和当前训练入口拒绝原因。
- `verify_curation.py`：从仓库根目录运行的复核脚本，只进行证据检查和已阅读的纯函数/临时目录样例核验，不启动GPU或模型训练。
- `session.json`：320无卡会话和关机状态；本轮未调用裁判API。

原始两份进度日志在文本导出时将回车CR规范化为LF。只恢复`Loading checkpoint shards`前的CR后，两份文件都与导出包记录的原SHA256完全一致。其余211份无此转换。没有通过改写原哈希掩盖传输差异。

## 复核实例

- `0022`：最终均值8.75正确，但过程把5+8+10+12写为45，存在算术错误，排除。
- `0198`：平均值Python函数把total赋为元组，执行除法抛出TypeError，排除。
- `0180`：要求8字，实际10个汉字；`0128`要求第3至17行即15行，实际16行，排除。
- `0088`：不能只看“请再提供3个”。完整历史已有海滩照片和字幕，教师却要求提供图片标题，没有完成追加字幕任务，排除。
- `0095`：初读时误判，逐字复核后确认慕改目得到正确的“耳濡目染”，最终保留。之前即时进度中的“改错字答案错误”不成立，以此最终记录为准。
- 保留项`0077`加法函数、`0151`去元音函数已实际执行；`0152`建目录脚本在临时目录验证成功。

其他完整逐条理由以manifest为准。前轮22条抽查记录保留为历史，不覆盖；本轮全量决定接续该记录。

## 当前训练条件与下一步

数据内容筛选完成，但以下两项尚未完成，因此`training_allowed=false`：

1. **训练入口支持可审计筛选子集。** 现有`scripts/distill.py::verify_target_review`强制训练输入属于原生成回执，并等于全部191条完整回答。实际用本轮152条输入验证，会得到`training targets are not bound to this generation`。后续应增加显式筛选manifest参数，先验证全部原始生成证据，再验证200条审核覆盖、保留/排除理由和筛选输出逐字一致；必须拒绝改写答案、遗漏审核和篡改哈希。原始生成回执及accepted=191保持不变，单独记录curated=152。
2. **用固定的真实0.5B学生tokenizer执行既有清洗和90:10分组划分。** 应复核清洗/截断/去重之后的实际数量、训练验证无交集和两轮训练步数。152只是清洗前数量，不能据此提前宣称最终训练/验证数量或步数。不能用字符烟测替代真实tokenizer证据。

以上通过后，才准备320 GPU上的两轮学生训练；之后还需要合并权重核验、CUDA冷加载、蒸馏前后CEval和同协议吞吐比较。152条来自一次200条可行性生成，不能据此宣称模型效果提升或具备上线条件。

## 会话收尾

本轮只启用320无卡模式回收文件，完成后已在AutoDL控制台确认已关机。原定关机为UTC19:30，已提前关闭；未启用GPU、未恢复旧监控。后续本地审核不继续占用实例。

复核命令（在仓库根目录）：

```bash
python3 reports/week8/phase3_distillation_curation/verify_curation.py
```


## 2026-09-18 学生训练准备续记

可审计子集训练入口和真实学生tokenizer清洗已完成：152条保留，137条训练、15条验证，提示交集0，教师答案全部保持原样；1条长输入截短后已复核。两轮共36次优化器更新。新增审核回执仅放行该数据输入，42项相关回归通过；尚未启动GPU训练。详见[学生训练准备记录](week8_distillation_student_preparation.md)。原有本轮之前的状态与哈希快照作为历史记录保留。
