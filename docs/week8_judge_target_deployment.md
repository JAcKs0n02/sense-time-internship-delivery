# Week8 裁判入口：320机部署核验

2026-09-16（Europe/London），通过已登录的Codex内嵌浏览器操作指定320机 be044ebe99-be706b14。实例原先关机，本轮以无卡模式启动：0.5核心CPU、2GB内存、无GPU，控制台价格0.10元/小时，最低消费0.01元。

## 已完成并核对

- 22个不含密钥的部署文件上传至实例，建立独立目录 `/root/autodl-tmp/week8-judge-runtime-20260916`，不覆盖旧目标运行目录中的代码及证据。
- 沿用的308个原始输入文件逐项SHA256一致；22个新增/更新文件同样逐项核对。
- 目标Python为3.10.20；裁判相关14项测试通过，用时0.181秒。这是目标部署子集，之前本机全量52项通过的结论另外保留。
- 目标机实际加载裁判配置并验证校准来源、请求参数、代码、评分表及题集哈希。
- 目标路径下重建正式配置，解析119科，304项依赖哈希全部一致。新候选运行锁继续为verified=false。
- 未加载模型、未占用GPU、未训练、未运行正式评测或付费裁判请求。

目标回执已完整回收，本地重建字节SHA256与目标打印值完全一致：`471df2329b1fb80184b549862c1bf371ae37871f9c5cb5ca540134812dddd9ec`。文件位于 `reports/week8/phase2_judge_target/target_receipt.json`。测试及配置构建原始日志仍在目标目录 `target-check`；本地回执保存日志哈希，不将其冒称日志全文已下载。

目标配置SHA256为 `4d5ae6d94d61e75066eb9533b6ea9d8fd04828e0833645966c5783a1e02a764f`，候选锁SHA256为 `7fee476670228fcf72753ca1b33ebe00f8ea42219a25ba69185f6921115db51b`。

## 尚未完成

目标机不存在 `/root/.config/internship-week8/deepseek.key`。本机Key没有混入部署包，也未复制到服务器。因此目标环境凭据与实际接口访问尚未核验，运行锁仍不放行。

已向用户明确询问：允许将本机Key单独传至这台320机的上述私有路径，或由用户自行录入。授权/录入完成后，只需先检查权限和接口连通性，再做最终运行锁复核；不需要重新跑已经通过的多轮评分校准。

## 会话结束

控制台已确认320机“已关机”。本轮无卡会话约5分钟，实际账单未查询；未持续占用GPU或CPU实例等待授权。

## 后续状态更新

用户随后授权单独传输Key，目标私有权限及官方只读接口核验已通过。此前“尚未完成”的凭据项现已解决，详见 week8_judge_target_credentials.md；历史原始回执不改写。
