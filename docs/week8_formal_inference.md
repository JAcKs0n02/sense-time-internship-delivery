# Week8 三模型正式推理会话

用户已于2026-09-16明确要求“开始正式推理”。本次使用320机`be044ebe99-be706b14`，RTX3090 24GB、14核CPU、60GB内存。平台开机报价¥1.48/小时。完整执行原基座、正式SFT、正式DPO，数据和推理参数沿用已审核协议。

## 执行范围与路径

- 每模型：CEval 52学科1346题、CMMLU 67学科11582题、custom20 20题，三模型合计38784条客观题和60条开放回答。
- 顺序：`original_base` → `final_sft` → `final_dpo`。每个模型调用已冻结的`step3_eval.py --mode fresh`，客观题完成并核验后生成custom20，再调用已校准DeepSeek裁判。
- 目标runtime：`/root/autodl-tmp/week8-judge-runtime-20260916`。
- 本次独立输出：`logs/formal-eval-20260916-01/`，协调日志：`logs/formal-eval-20260916-01-coordinator.log`。
- 独立模型锁：`reports/week8/phase2_three_model_eval/{模型名}/runtime_lock.json`。上一阶段三份锁已通过实际入口检查，本次入口仍重新计算依赖哈希。

本轮操作脚本在本地`reports/week8/phase2_formal_inference/run_session.py`，部署到目标`/root/autodl-tmp/week8-target-runtime-20260915/three-model-cpu-audit-20260916/run_session.py`。SHA256：`92a46595b35c1bbf1efa7f09e763d187ccb007ceda589379ccfb294594fc3a33`。它只协调已有正式入口，不修改题目、模型、裁判或评分实现。

## 执行保护

启动前核对指定主机、空闲GPU、BF16支持、三个锁的哈希、至少10GiB空闲盘和独立输出目录。使用进程锁防止同一协调器并发启动。每模型仅启动一次，任一失败立即停止后续模型并保留证据；不覆盖旧输出、不自动重试。切换模型时写入各自锁，会话结束恢复原始预检文件。

平台定时关机：2026-09-17 **10:15 UTC**（页面显示11:15）。入口截止10:10 UTC，超时终止本任务子进程组并写失败记录。接近12小时是兜底窗口，不是预计一定运行12小时；按1.48元/小时约17.76元，不含存储等其他费用。实际吞吐会用于核对窗口是否充足；若明显不足先报告，不擅自降题量。完成或失败后监控负责回收与关机，避免空闲等到保护时限。

沿用已有`week8-320`监控，每5分钟检查本次推理状态。运行无实质变化时安静，阶段完成、异常或需要用户处理时通知。原训练监控提示已替换，禁止重启训练。裁判沿用每模型最多40次尝试及原余额保护，不展示密钥。

## 验收标准

“进程已启动”不等于“评测完成”。需要三模型各自的完成状态、119学科完整逐题结果、60条开放回答及评分，并独立核验标准答案、覆盖、分数、裁判响应和用量。此后才能生成比较报告；SFT验证loss上升的事实须继续保留，不能预先声称训练提升效果。

本次启动证据与最新观察保存在`reports/week8/phase2_formal_inference/`；最新运行状态以目标机日志及其采集时间为准。

## 启动核验

2026-09-16 22:15:38 UTC协调器PID1497启动。22:18:16 UTC观察：原基座阶段RUNNING，已产生6份学科预测JSON；推理日志可见逐题进度，参数为max_new_tokens=32、do_sample=false、num_beams=1。GPU显存15648MiB，利用率94%。裁判评分尚未开始；预测文件的出现不是已完成评分的证明。

`formal_inference_start.json`已从目标机下载，SHA256为`3c7c36862f9fa13c17d45cb800cefbd9b7c1c2824d417b6914a82cb809ecd374`。脚本身份、运行状态与真实产出已本地核对。监控已设为ACTIVE，平台关机时间已通过页面确认。

## 2026-09-16 23:50 UTC：失败收尾与独立核验

原基座在23:43:02 UTC因逐题明细格式兼容问题退出，协调器停止后续模型并恢复原预检文件。并非GPU推理失败：119学科、12,928题预测和评分文件均已生成。实际CMMLU的`details`是列表，且`pred/refr/is_correct`是单元素列表；当前入口仅接受字典明细及标量字段，因此报“missing benchmark details or golds”。错误文案不足以准确描述根因。

已回收374份产物，归档SHA256 `089db59f8613651821ec4dcfa756edb238063f45eb9473bc87e1ddcd8af0754f`，本地逐项核对manifest的大小和SHA256全部一致。独立按冻结标准答案、学科覆盖、题目顺序/编号、逐题判断及汇总分数核验：CEval 1049/1346（77.9346%），CMMLU 9246/11582（79.8308%）。这只是原基座客观题结果，不能替代三模型完整验收。

custom20未生成、裁判调用0次，SFT/DPO均未启动。确认本任务进程全部退出、GPU无计算进程后，已关闭320机，页面确认“已关机”；残留定时已清除，监控已暂停。未自动重启或重跑，原始证据未修改。详见`reports/week8/phase2_formal_inference/failure_review.json`和`recovered_failure/`。

下一步应在本地用真实CMMLU结果建立失败回归用例，修复列表明细与单元素字段的严格校验，重新审核依赖锁，并设计复用现有客观题结果的受控续跑入口。未经核验不得直接改变冻结锁或重跑整个原基座。当前生产验收代码尚未修复。

## 后续本地修复

CMMLU列表格式校验已修复，并通过真实119学科12,928题离线复验。详见 `docs/week8_eval_schema_recovery.md`。旧放行锁尚未更新，受控续跑入口尚未实现，GPU与监控均未重启；上述失败记录继续作为历史证据保留。
