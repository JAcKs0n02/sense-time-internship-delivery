# 第8周第一阶段任务6：复核与交付冻结

日期：2026-09-15。结论：**PHASE1_DATA_PREPARATION_PASS_TRAINING_BLOCKED**。

任务1–6的数据准备阶段已完成，可以进入下一阶段的环境及输入预检。这个结论不等于DATA_READY_FOR_TRAINING：benchmark实际快照、裁判与评估运行配置尚未锁定，新增题目污染检查及GPU/完整权重身份复验仍须在任何新训练前完成。训练入口的阻止逻辑保留。

## 前5步重新核对

本轮重新执行检查，没有仅引用上一次的通过结论。全部75项汇总检查通过，详见[本轮收据](../reports/week8/phase1_task6/verification.json)。

| 步骤 | 本轮复核 | 结果与边界 |
|---|---|---|
| 1 输入核定 | 重跑源审计，重新核对父集合、血缘、历史排除和依赖哈希 | 34项通过；1663=1580+83；源审计及清单与原记录字节一致 |
| 2 协议冻结 | 核对规则文档、源、血缘、历史分组、保留集、5份tokenizer文件与协议哈希 | 冻结身份未变；评估方法已固定，运行身份仍待锁定 |
| 3 全量准备 | 重新执行独立导出审计，包括真实tokenizer长度、双格式、跨集关系、保护题及复跑哈希 | 1422/158；原文1580条未变；504条去重保护题词面命中0；15份JSON复跑一致 |
| 4 内容抽查 | 核对50条抽查与当前正文、全部13条多轮覆盖、审查记录完整性；重跑样例执行 | 13项样例执行通过；41条保留、9条带提示保留；没有新增全量事实审查或人工签名 |
| 5 真实加载 | 在新目录重新运行LLaMA-Factory CPU加载、独立监督检查、真实Alpaca等价检查 | 全部1580条通过；1599轮/94298监督token；两份全量token行文件与任务5字节一致 |

另外重新运行27项回归测试，全部通过。记录见[测试日志](../reports/week8/phase1_task6/recheck/tests.txt)、[独立数据审计](../reports/week8/phase1_task6/recheck/task3_verification.json)、[真实加载目录](../logs/week8-real-loader-20260915/task6-recheck/)。

本轮第一次独立导出复核误用了未装transformers的项目测试环境，未能完成；保留[失败日志](../reports/week8/phase1_task6/recheck/task3_wrong_environment.log)。改用任务5锁定的CPU环境后全量重跑通过。这个失败没有被覆盖或算作通过。

## 冻结交付

[本阶段快照](../deliverables/week8/phase1/)包含：

- 当前1580条源及血缘、83条保留集、协议引用的保护题和分组文件；每份明确原路径与SHA-256。
- 四份双格式数据、dataset_info、统计、长度、问题组、排除及血缘等15份JSON。
- 冻结协议、5份tokenizer和原始Qwen架构config；不包含adapter或模型权重。
- 数据处理与验证脚本、测试、依赖配置、任务1–5证据，以及本轮复核收据与实际token/label行。
- 下一阶段计划，含候选SFT基座、SFT/DPO输入、短试跑步数、输出隔离和待验条件。

[快照清单](../deliverables/week8/phase1/manifest.json)给出每份文件的原路径、归档相对路径、大小和哈希；SHA256SUMS覆盖快照负载及manifest。归档是本地证据冻结，不是对外发布、Git提交或训练放行。快照保留repo相对路径，原验证脚本仍按仓库根目录解析依赖；它不是脱离仓库即可直接启动的训练安装包。历史审核的大型上游依赖以输入清单中的路径/哈希引用保留，不宣称复制了整段实习仓库。

原机器协议及任务1–5收据记录各自完成时的状态，保持原字节不变。本轮状态以任务6收据为准；没有为了改成PASS而篡改旧协议的pending字段。

## 下一阶段如何开始

详见[机器可读接续计划](../reports/week8/phase1_task6/next_stage_plan.json)。按以下顺序推进：

1. 固定C-Eval/CMMLU实际数据、软件、配置、few-shot和裁判身份；将实际题目加入污染检查。若需要剔除样本或更改模板，升版并重跑任务3–5。
2. 在目标Linux GPU机器核验CUDA、PyTorch、量化依赖、可用显存和完整原始基座。候选模型是Qwen/Qwen2.5-7B-Instruct；历史路径仅用于定位，必须重新核对全部权重分片、索引、版本及config，不能把本次只核对config当作权重验收。
3. 通过上述条件后，SFT短试跑使用本次1422/158数据、冻结tokenizer、qwen模板、2048上限、seed/data_seed=42、所有assistant监督。计划3个optimizer steps，batch=1、梯度累积4、无checkpoint恢复；不从旧恢复模型继续。
4. DPO候选为Week4修复版783/87偏好数据。它尚不属于本次SFT验收范围：先检查chosen/rejected、与SFT验证集及评估题的交叉、真实pairwise loader和长度，再用已核验的短试跑SFT合并产物做3步技术联调。
5. 短试跑与完整训练使用不同的新目录；试跑权重不接续正式训练。完整SFT从原始基座开始，完整DPO从验收后的正式SFT合并模型开始。完整超参数与步数另行冻结，不能直接照搬CPU验收配置。

本次未启动GPU、加载完整权重、训练或调用外部裁判。

## 保留的局限

50条分层抽查不能保证1580条每个事实正确；9条保留提示仍存在。词面去重不证明无语义泄漏。新验证集不适合给已训练过原1580条的旧恢复模型报告未见数据泛化。CPU loader通过不等于模型forward、损失或CUDA训练通过。这些均作为后续结论的边界保留。
