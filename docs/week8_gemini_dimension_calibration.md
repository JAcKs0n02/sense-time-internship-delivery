# Gemini评分维度归因校准（2026-09-17）

原失败点：MISSING-REASON同时缺少必要依据与违反恰好3条的格式约束，旧操作口径未明确交叉归因。保留旧批次失败状态，本次不改变五维权重、不改原14例阈值。

新规则：内容覆盖与结构约束分别检查。合并字符串但语义完整只影响格式；条数合规但重复导致缺实质内容只影响完整性；两者都违规分别指出证据扣分。明确数量不符且主体可用的格式至多3分。

新增隔离题使用5个样本、8%提升，显式要求覆盖样本不足、不确定性估计缺失、无对照组三个依据：
- ISO-FORMAT：三个依据合并为两个字符串；准确性≥4、完整性≥4、格式≤3。
- ISO-CONTENT：三个字符串，其中一个重复并遗漏无对照组；准确性≥4、完整性≤3、格式=5。
- ISO-BOTH：两个字符串并遗漏无对照组；准确性≥4、完整性≤3、格式≤3。

先三个隔离题，再原14题，共至多17次；Gemini 3.1 Pro Preview、high、16384输出上限、零自动重试。所有输入与代码冻结manifest，SHA256为4e9158aa94be04e18eb33368e73fee7abbef2f7db8ce39117ee3549c63dcca8d。失败立即停止，不能修补返回JSON或修改本轮阈值追求通过。

证据目录：reports/week8/phase2_gemini_calibration_v2。新规则是对既有rubric的操作化定义，属于项目评分协议，不声称来自教师逐字指定。此为诊断校准，不能用来估计独立测试集上的裁判准确率。即使校准通过，仍须适配正式仅评分入口并验证来源绑定、防重复与失败恢复。

## 实际结果

17例全部通过独立复核，两组配对各维与加权差均为0。17次调用、零重试，总用量52880 token。该协议已经用于现行评估入口与三模型正式评分。详见同目录verification.json与judge_profile_candidate.json。
