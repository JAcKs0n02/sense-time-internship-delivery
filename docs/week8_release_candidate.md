# Week8 独立发布候选与 CPU 验收

> 2026-09-19验收口径更正：老师要求干净环境至少运行数据准备与评估，并支持分段执行；没有要求以Linux CUDA重跑训练、蒸馏、部署全流程作为本项验收。下文历史“完整CUDA复现”待办由[当前准备方案](week8_clean_eval_preparation.md)取代；历史实验回执和冻结候选不改写。

2026-09-19：本地独立发布候选已完成，CPU 入口及全部 170 项测试通过；尚未推送，也未放行最终发布；干净环境真实评估尚未完成。

## 候选范围

目录：`outputs/week8-release-candidate-20260919/repository/`。逐文件源白名单共 5922 项，复制内容约 614 MB，另外生成导出说明、校验表与验收摘要。它包含现有代码、文字证据、必要冻结输入，以及已核验的最终技术报告和六幅配图。白名单和每项来源哈希见 `reports/week8/release_candidate_20260919/allowlist.json`。

不复制 Git 历史、虚拟环境、缓存、模型权重、压缩包和多数历史图片/视频；这些附件不在本候选的完整性承诺中。原有历史文档仍可能引用未纳入的媒体、远端路径或模型，本候选不是完整导师附件包。权重已独立备份，Week7 媒体按既有资产清单另外管理。

正式数据协议引用了旧 `outputs/`、`.worktrees/` 和 `logs/` 路径。候选只按明确清单保留其中必要文件，保持冻结路径及内容哈希；没有复制 Git 工作树元数据。候选专用忽略规则经过独立空 Git 目录检查，5922 个白名单文件均不会被误忽略。原仓库的忽略规则只新增了候选输出目录，不改变原始数据的跟踪状态。

## 脱敏与实验身份

9 个历史 JSONL 文件共 18 处已知凭据字面量被替换为 `[REDACTED_API_KEY]`，同时覆盖 OpenAI 格式和 Google 格式。逐文件记录原哈希、导出哈希、行号和数量，不保存凭据原文；原仓库 9 个文件哈希保持不变。

逐行解析确认行数、JSON 结构及其他字段内容不变。这些脱敏副本用于发布和历史数据入口检查；原有训练成绩仍对应原始实验，没有用脱敏数据重新训练。冻结正式输入、评分协议和评分证据没有被改写，不能通过重算历史身份哈希来掩盖差异。

候选内原有 `SHA256SUMS.txt` 另存于 `release_metadata/historical_checksums/`，原位置重新生成，只覆盖候选中实际存在的文件。历史 JSON 身份回执继续保留原值，当前导出文件完整性以根目录 `RELEASE_SHA256SUMS.txt` 为准。

## 实际验证

新建独立 Python 3.11 环境，安装数据依赖、PyTorch 2.5.1、OpenCompass 0.5.3、MMEngine 0.10.7、pytest 等，`pip check` 通过；实际完整版本快照随证据归档。测试不加载用户模型或访问 GPU，不调用裁判。

验证进程启用 Python 访问保护：禁止读取候选之外的用户目录文件，禁止非回环网络连接，使用空 HOME 和离线 Hugging Face 配置。单独负向探测确认保护生效，再执行以下验证。

| 检查 | 结果与边界 |
|---|---|
| `run_pipeline.sh --quick` | 通过；脱敏历史数据清洗与历史评分回放，不是新模型推理 |
| `run_pipeline.sh --score-only all` | 通过；三模型各 20 题既有评分核验，新增 API 调用 0 |
| 冻结协议正式数据准备 | 通过；1580 条输入，1422/158 划分，隔离命中及跨集合分组均为 0 |
| 正式数据重现 | 15 个输出 JSON 与原始正式准备结果逐字节一致 |
| 全量 `pytest tests` | 170 passed，1 warning；包含实际 OpenCompass 模板和真实 CPU 张量校验 |
| 依赖完整性 | `pip check` 通过；不代表 Linux CUDA 依赖已完成整体验证 |
| 候选扫描 | 全量可解码文字文件扫描已知格式；DOCX 的 XML 和 PDF 提取文字另查；具体文件数与范围见最终回执 |

第一次导出暴露了冻结数据、加载器 tokenizer 和恢复日志遗漏；数据环境也缺少张量/评估依赖。补齐明确依赖后重新导出并执行全部检查，保留首次失败和最终成功记录，没有跳过最终测试或更改原有校验门槛。第一次 API 字面量识别遗漏 Google 格式时，导出在写文件前失败；修正后对 9 个文件重新全量核对。

模式扫描不是对所有敏感信息的证明：未扫描原仓库完整 Git 历史；历史 notebook 可含嵌入输出，保留的历史文字也可能包含一般姓名、路径等信息。正式公开范围仍需最终发布审查。本轮未扫描或上传未纳入的压缩包、模型和历史媒体。

主要证据：[总验收回执](../reports/week8/release_candidate_20260919/verification.json)、[脱敏映射](../reports/week8/release_candidate_20260919/redaction_mapping.json)、[运行记录](../reports/week8/release_candidate_20260919/validation_runs.json)、[15 个输出一致性](../reports/week8/release_candidate_20260919/formal_reproduction_comparison.json)。首次候选另存于同级 `repository-initial/`，只用于追溯，不作为当前发布候选。

## 下一步

以此候选为基础准备干净环境的数据准备与真实评估，跳过已完成的训练、蒸馏和部署。先完成当前fresh入口与Gemini新评分的接入，再形成新的候选与目标环境运行锁。见[准备方案](week8_clean_eval_preparation.md)。本次未修改已冻结候选，其原有哈希和历史验收记录继续有效；本次修订需纳入后续候选。历史源仓库及 Git 历史仍需在确定发布范围后单独审查；不应直接对原仓库执行全量暂存推送。本轮没有启动320，没有删除检查点，也没有提交、推送或向导师发送文件。
