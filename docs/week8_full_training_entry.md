# Week8 正式训练入口：本地验收记录

日期：2026-09-16。本地实现与审核记录；后续完整训练、合并、独立加载及证据复核已完成，最新进展见 `docs/week8_full_training_target.md`。以下测试结论仍仅指本地验收。

## 费用与训练效果

按最新要求，训练效果优先，不为了压低费用减少既定数据、轮数、序列长度或关键参数。保留SFT 5轮、DPO 40步，以及两次合并和独立加载检查。节省来自避免GPU空闲、输入检查失败时不开训、失败后停止和避免重复无效实验。

上一轮¥4.44/3小时属于初步建议，已由本原则替代。当前候选release仍含10800秒保护值；部署前必须按实际训练吞吐和合并耗时复核，留足执行窗口后重新生成实际release及哈希。这个值不是压缩训练的目标，也不是完成耗时保证。不能明知时间不足仍开训再从头重跑。保护期限内未完成的运行必须记为不完整，不能缩短训练后算通过。

## 这次完成的部分

1. `scripts/week8_full_training.py`：独立正式入口。冻结40个文件（包含原31项输入），校验原smoke锁及复核结论、四份候选配置、实例和模型来源。已有输出、哈希变化、未启用的release和过期关机保护均拒绝执行。
2. 训练启动时核对预计SFT 1775步/1422条、DPO 40步/783条；正确区分训练和验证日志。每阶段只启动一次，失败停止，不自动改变超参数重跑。
3. `scripts/week8_full_verify.py`：在受时限约束的独立CPU进程检查完整步数、有限训练指标、每轮/指定步验证记录、非零LoRA更新及优化器状态，再核验合并分片和文件身份。
4. `scripts/week8_full_cold_load.py`：新进程加载独立BF16合并模型，对“你好”执行前向和最多2个token贪心生成。必须有有限logits和有效输出，才进入后续阶段；这不是质量评分。
5. 每个子进程保存开始/结束时间、退出码、错误、超时状态和日志哈希。重计算检查也受期限控制；失败只终止本任务进程组。

执行顺序：SFT → adapter验收 → SFT合并 → 合并验收 → 独立加载 → DPO → adapter验收 → DPO合并 → 合并验收 → 独立加载。全链路通过后仍标记为需要回收复核，不自动开展正式评测。

## 验证证据与边界

- `reports/week8/phase2_full_training_entry/regression-tests.log`：77项Week8测试通过，其中新增16项。覆盖真实小张量adapter/optimizer检查、CPU worker实际执行、真实子进程失败/超时清理、10个阶段的注入失败阻断、dry-run及输入门禁。
- `week8-full-entry-red-*.log`：开发过程预期失败记录。包含训练/验证同名样本字段误判的复现；修复后对应检查通过。这些历史失败日志不是当前运行失败。
- 独立代码复审修复了启动参数检查、CPU验收时限、失败回执及验证样本误判，最后复审未发现剩余阻断项。结论见 `code_review.json`。
- `candidate_release.json` 保持 `execution_enabled=false`；真实CLI dry-run只输出计划，未启动训练、GPU或API调用。
- `formal-entry-candidate.tar.gz` 内40个输入及release已逐项核验，摘要见 `verification.json`。解包应使用全新目标目录，不覆盖已有训练资产。
- 本地小张量、模拟日志及失败注入不能证明7B模型真实训练、合并或GPU冷加载已经通过。质量效果也必须等待后续CEval/CMMLU及custom20评测。

## 下一步

只检查已确认的AutoDL 320机。部署前复核运行窗口、平台关机保护、可用GPU、至少45GiB磁盘、依赖与原始基座身份；将候选包部署至独立目录并重新核验。实际release绑定当前关机期限后启动完整SFT，按上述门禁依次推进DPO。若GPU暂无空闲，保持关机，恢复监控时必须更新旧smoke任务提示，不能直接运行旧监控。

本阶段没有启动GPU，没有改变已暂停监控，也没有调用DeepSeek。原始smoke证据和冻结脚本未修改。
