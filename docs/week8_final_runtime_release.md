# Week8 最终评估运行锁复核

本轮重新审核当前证据，不沿用早期“裁判未选定”的历史汇总状态。基座模型、数据、真实提示、目标前向、裁判校准、正式入口集成、目标部署和私有凭据的证据分别核对。

## 已复核内容

- 当前索引中基准输入、重叠排查、独立验证、目标前向、裁判校准/集成/部署/凭据收据的SHA256均一致。
- 重新生成目标配置，字节SHA256与320机回执完全一致；候选运行锁同样逐字节一致。
- 304项配置依赖重新核对。当前推理配置与前向验证版本相比，仅工作根目录发生变化；归一化目录后的完整模型/数据配置相同。
- 已选定的DeepSeek配置、代码、题集、rubric及校准回执通过正式入口配置校验。

## 放行范围

此次仅放行绑定原始Qwen2.5-7B-Instruct路径的评估运行锁。更换为新SFT或DPO合并模型时，必须分别绑定新模型身份及路径，不能继续使用原始基座的锁。

该锁不启动评测，不代表模型分数已产生，不放行完整训练。训练入口目前仍存在旧数据阶段的硬性拒绝分支；后续需将其替换为可验证的短程训练准入检查，不能仅删除该分支。

## 3步测试安排

计划保存在 reports/week8/phase2_final_release/smoke_plan.json：SFT恰好3个优化步，保存adapter并合并独立的测试SFT模型，再以该模型进行DPO恰好3步。保留2048长度、4bit QLoRA、rank8和seed42；关闭周期验证，限定保存与记录步数。需要验证有限loss/梯度、实际优化器更新、global_step=3、adapter完整与SFT→DPO参考关系。

短程测试产物不得命名或宣称为正式训练完成模型。最终成绩仍需完整训练后重新评测；模型服务别名和已观察0.5分重复差异的限制继续适用。

本轮证据目录为 reports/week8/phase2_final_release/，正式训练开关保持false。

## 320机实际执行结果

2026-09-15 23:41:10 UTC，在320机无卡模式下完成应用。目标状态为 `TARGET_BASE_EVALUATION_LOCK_RELEASED`，实际调用正式入口的 `require_evaluation_lock` 校验通过，307项文件通过核验。没有启动推理或训练，API调用次数为0。

目标回执已回收为 `reports/week8/phase2_final_release/target_receipt.json`，SHA256为 `6c43d3ac7886ed1482d5e2d72cefcb400027890479687b0e5f6e324213da61ae`，与目标终端输出逐字节一致。回执绑定的运行锁和复核文件SHA256也在本地再次核对通过。

本地 `configs/week8_evaluation_preflight.json` 已更新当前状态和证据索引，保留历史证据文件；目标机运行锁仅通过 `target_runtime_release` 引用，没有将目标机专用路径误设为本地可运行配置。

下一项是实现和审核3步训练准入，再在GPU上执行SFT→合并→DPO短程测试。当前只有测试计划，尚无3步训练完成证据；完整训练仍未放行。

收尾已在AutoDL控制台确认320机为“已关机”；关机时显示“GPU充足”，不保证下次启动时仍可用。会话记录见同目录 `session.json`。
