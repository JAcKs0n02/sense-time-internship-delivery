# Week8 按老师原文重新核对：完成度、超额工作与最小收尾

> 当前安排更新（2026-09-19）：用户明确暂不考虑删除。检查点清理从当前执行计划移出，未删除任何文件，不再等待删除确认；教师45.1不能因此标记为已完成。现行依赖与本地独立检查已完成，报告和提交说明本轮改用Markdown主稿及LaTeX/PDF同步。当前剩余为：未验证的全新CUDA环境范围、Week1–8交付仓库整理、Git发布及访问检查。已有新目录GPU实测不等同于从零建立依赖环境。

核对及入口更新日期：2026-09-19。用户截止时间：英国Europe/London，2026-09-20（周日）23:59；计划周一早上由用户提交老师。核对后已修正交付入口，并完成单模型限定评估：119题基准样本＋完整custom20；320已关机。未重训、删除模型或推送代码。

依据为根目录《实习需求.docx》Week8，SHA256：a9d6d1cd16f3f5acdf6c96f1500f3597e337b08ea276021645d49e05639bc526，与既有来源记录一致。旧执行计划和验收清单不是新的教师要求。本文件同步收尾范围和当前执行进度。

## 现行依赖与本地交付更新

新安装macOS Python3.11环境和独立交付副本已完成检查：现行评估依赖安装及pip check通过，正式数据15个产物逐字节一致，评估配置、已有评分核验及6项入口测试通过。训练补充依赖按320实测版本对齐；旧哈希绑定文件保留。本轮未运行GPU或API，未验证全新Linux CUDA安装。详见[本轮结果](week8_dependency_delivery.md)。

报告与提交说明已同步最新状态，老师允许Word/LaTeX草稿及PDF源文件终稿，本轮采用Markdown主稿、LaTeX和PDF。下一步为Week1–8导航整理与Git交付。以下实验阶段记录按原日期保留；其中曾安排的删除工作已由用户明确暂缓。

## 结论

训练、三模型评测、部署、蒸馏及比较、报告主体已经完成。交付入口已修正，通过本地检查及320新目录真实评估。该GPU实测复用既有CUDA环境，未从零安装。尚未完成的主要是：全部周次仓库整理和Git交付；检查点删除按用户决定暂不处理；依赖安装的未验证范围须在提交说明中保留，不能把本轮称为从零重建CUDA环境。

此前把完整Linux CUDA全流程重跑、不断追加运行锁/候选与工程审查当作必经步骤，扩大了任务范围。应停止新增研究实验和工程功能，只关闭老师要求中的实际缺口。已有加固保留使用，不为“简化”再花时间拆除。

核对发现并在本轮修正的两个入口问题：

- 原step2/default入口拒绝protocol_formal_tokenizer正式数据。现由scripts/pipeline/step2_train.py核对已验收数据并生成可迁移配置，step2_train.sh和主控已接通；未重复训练。
- 原默认评估分支仍依赖旧DeepSeek方案与缺失的runtime_lock。现改为scripts/pipeline/step3_eval.py，使用现有Gemini协议；本地计划、汇总和历史兼容检查通过，真实GPU路径已于本日限定范围实测通过。

## 老师的17项具体要求逐条对照

| 编号 | 老师要求 | 当前成果 | 完成度和剩余部分 |
|---|---|---|---|
| 40.1 | step1_data_prep.py：加载原始数据，清洗、去重、Alpaca/ShareGPT，随机9:1划分，统计JSON | 正式1580条输入；1422训练/158验证；双格式、去重与防泄漏检查；独立副本15个产物逐字节重现 | 已完成。原文允许“下载/加载”，已有原始数据无需重新下载 |
| 40.2 | step2_train.sh调用LLaMA-Factory，按预设最优参数依次SFT/DPO，日志保存logs | 真实SFT1775步、DPO40步；现行step2/default主控接通正式数据与已实测参数 | 训练成果已完成，入口配置检查通过；本轮未重复训练 |
| 40.3 | 错误重试，例如OOM降低batch；训练后模型合并 | 现行入口复用有限OOM重试；两个模型实际合并和冷加载通过 | 已完成。batch已为1时停止并报告，不静默削减轮数或长度；无需人为制造GPU OOM |
| 41.1 | step3_eval.py加载合并模型，运行CEval/CMMLU，20题自动五维评分，输出CSV | 原基座/SFT/DPO各有全量基准及20题Gemini成绩；现行入口集中到scripts/pipeline/step3_eval.py | 现有实验、评分和CSV已完成；新入口119题基准样本＋完整custom20真实运行及CSV核验完成；不替代历史全量成绩 |
| 41.2 | step4_deploy.sh后台启动量化vLLM、Gradio并检查健康 | 320真实vLLM API、Gradio客户端对话、health、重启与异常回收通过，48份证据 | 已完成。老师没要求服务永久在线或必须新录浏览器演示 |
| 41.3 | run_pipeline.sh支持--skip-train、--skip-eval分段 | 两个默认分支已接通；quick、正式数据及配置生成、既有评分核验通过 | 数据准备→GPU生成→本地Gemini评分分段实测完成；复用既有CUDA环境 |
| 42.1 | 学习软标签、温度等蒸馏原理 | 报告第8章解释软标签、温度、KL与序列级蒸馏差异 | 已完成。未把序列蒸馏冒称逐token KL |
| 42.2 | Week4 DPO教师、Qwen2.5-0.5B学生；允许自行实现；仅2轮 | 使用Week4教师，0.5B学生，152条筛选教师样本；137/15划分，2轮36步，配置与日志齐全 | 已完成。原文允许自行实现，不必另做一套KL蒸馏 |
| 42.3 | 比较学生前后CEval和推理速度，分析效果 | CEval53.7147%→53.1204%；55.5303→55.6263 token/s；同协议比较及原因/限制分析齐全 | 已完成。负结果允许，不需要继续训到分数提高 |
| 43.1 | 第1–4章：环境架构、数据工程、SFT、DPO | 四章内容、硬件选型、数据与实验结果齐全，现行Markdown/LaTeX源存在 | 已完成 |
| 43.2 | 每章图表，前四章累计≥3000字 | 各章均有图或表；前四章超过5000中文字符；LaTeX中图表自动编号 | 已完成。无需为“每章配图表”另外给每章都画一张图片 |
| 44.1 | 第5–8章：多模态Agent、部署量化、Pipeline、总结 | 八章齐全；有量化显存/速度数据、Pipeline图与使用例、蒸馏比较 | 已完成 |
| 44.2 | 润色、统一图表编号与参考文献，≥6000字，PDF与源文件 | 超过1.5万中文字符，现行LaTeX/PDF与源文件；6幅图与21张编号表；核验见latex_report_20260919 | 已更新入口实测、依赖验证、备份及交付状态；旧Word/40页PDF仅作历史版本 |
| 45.1 | 清理所有中间检查点，仅保留最终SFT/DPO/蒸馏各一份 | 最终三模型38文件31.50GB已备份并验证；未执行中间checkpoint清理 | 未完成。列明可删检查点并清理；不能把独立备份或Week7量化服务资产误当训练中间点删除 |
| 45.2 | data/scripts/configs/models/logs/reports功能目录 | 六类目录已存在，历史归档与现行工程并存 | 功能分区已有；按用户要求，提交前仍需统一全部周次报告与目录、分开过程档案和现行成果 |
| 45.3 | README含简介、conda env create、run_pipeline.sh --quick、FAQ | 所需章节/命令齐全，quick独立运行通过，GPU环境另列依赖 | 基本完成，需最后同步当前入口、依赖及完成状态。基础environment.yml只供quick，不能暗示一键安装了所有GPU阶段依赖 |
| 45.4 | 所有代码推送Git、忽略大模型、交付仓库链接 | 已有远端地址、ignore和独立脱敏候选；Week8脚本/报告等仍大量未跟踪/未提交 | 未完成。选择最终提交内容、提交推送并确认老师可访问。已有Git网址不等于本周工作已上传 |

## 四项验收标准

1. 原话：“自动化Pipeline在干净环境中（提供依赖清单）可无报错运行（至少包括数据准备和评估步骤）。”
   - 已有：独立CPU环境的数据准备、历史评估回放/重计分和196项测试；另有实际GPU训练/评估的独立记录。
   - 当前边界：不能把历史重计分称为新模型推理；新入口已在320新目录完成119题基准样本＋完整custom20，再由本地既有独立Python环境完成20次Gemini评分；本轮未验证从零安装CUDA依赖。
   - 边界：老师没有写Linux，也没有写必须在这个验收中重做训练、蒸馏、部署或重新比较三个模型。是否重跑整套12928题是执行方案选择，不能伪装成老师原话。已有全量分数继续有效。
   - 最小建议：先修入口，核对已有GPU环境记录能否直接作为交付环境依据；如仍缺真实路径验证，针对一个已有模型补一次限定范围的数据准备+真实评估。样本级运行只证明入口可运行，不替换已有全量成绩，也不能标成全量基准复现。
2. 原话：“技术报告结构完整，字数≥6000字，图表清晰，逻辑严密。”主体已达到，本轮已同步状态并更新LaTeX/PDF。
3. 原话：“代码仓库结构规范，README可指导他人快速复现。”目录已有，入口说明与推送尚需收尾。
4. 原话：“成功进行蒸馏尝试（即便效果不理想，需有分析）。”已完成，准确率下降不构成继续训练的义务。

本核对没有擅自把仅历史回放判为真实模型验收，也没有把“一次Linux CUDA从训练到部署全跑”增加为教师标准。

## 超出Week8明确要求的工作

| 已做或曾计划的工作 | 与老师要求的关系 | 收尾决定建议 |
|---|---|---|
| 原基座、SFT、DPO三模型完整评测、多次细分诊断 | Week8要求自动评估，未明确要求三模型再做全套比较 | 已有结果保留；停止新增重复比较 |
| 本周新增两份各60条人工评分 | Week8没有再次双人盲评的独立条款；Week3的要求不自动变成本周重复义务 | 已完成就归档；不再麻烦用户评分 |
| 多轮裁判校准、不同供应商切换与稳定性分析 | 老师只规定自动五维评分，没有指定API、裁判模型或校准轮数 | Gemini是实施选择；已有协议继续使用，不再升级/重选/追加校准轮次 |
| 要求模型效果提高、证明可扩大上线 | Week8未设质量提升硬门槛，蒸馏明确允许负结果 | 不再以提分或上线作为结束条件 |
| 完整Linux CUDA训练→评估→蒸馏→部署再次重跑 | 老师最低干净环境验收只要求数据和评估，并允许分段 | 从必做清单删除 |
| 大量运行锁、历史身份绑定、反复候选导出、独立审查/故障注入 | 属于工程保障，超过教师明确交付深度 | 现有成果保留；不继续追加新的保障体系与“通过后再多一层”条件 |
| 196项测试、全量SHA与多次独立CPU冷加载 | 有价值，但老师未要求该测试规模或反复取证 | 不再为刷新测试数字重跑整个项目，修改后只做必要检查 |
| 重新做全量Git历史审计、长期生产级服务运维 | 不是本周明文交付 | 只处理本次推送真正包含的代码、已知凭据与大文件；不无故扩大历史重写或运维范围 |

必须保留的基本正确性不属于可以省略的“额外工作”：不把密钥和大权重推上仓库、不伪造评测/训练结果、不把缺失题目当0分、未知收费请求不自动重复、删除中间模型前确认最终模型已保存。

## 最小收尾计划（第一项已执行入口修正）

截止为周日英国时间23:59，只保留四个工作包：

1. **统一老师要求的运行入口，解决验收1/3的实际缺口。** 优先修step2/default主控与正式数据不兼容、旧step3/新Gemini入口不一致；给出明确的安装和分段命令。复用已完成训练及成绩，不重训、不添加新框架。视已有环境证据，最多补一次单模型干净环境评估验证；不自动升级为全流程重跑。
2. **完成独立交付副本与依赖对齐。** 本轮已完成新macOS环境的本地检查，继续在同一副本上同步文档与目录；保留未验证的CUDA范围。检查点删除已按用户决定从当前安排移出。
3. **冻结报告与提交说明。** 修正过期状态，补最终命令/路径；同步Markdown、LaTeX和PDF，做渲染检查，不重写八章或新添实验。
4. **提交并推送Git，整理给老师的入口。** 复用已完成脱敏的文件范围，确认代码、报告、配置、必要日志可访问；提供一个仓库入口和报告PDF/源文件。周一由用户提交。

建议排期：周六集中处理第1项；周日中午前结束必要的最后运行；周日下午处理2/3；周日20:00前完成推送与材料核对，保留至23:59的故障缓冲。耗时是排期建议，尚未承诺具体GPU可用时段。

若某项实测失败，应准确限定并处理失败项，不能未经用户同意追加一轮研究或默默扩大范围；若因此无法按时满足要求，必须如实报告尚缺内容，不能改写成通过。

## 2026-09-19入口修正阶段记录与仓库整理约定

用户已要求开始第一项，并明确在需求完成后统一整理全部周次的仓库结构。当时仅调整交付入口；后续真实运行结果见下节。本轮始终未重训、删除检查点或创建远端仓库。

入口代码已集中到 `scripts/pipeline/`：主控通过 `scripts/step2_train.sh` 接收经过核验的正式数据，使用已实测参数顺序训练与合并；现行 `step3_eval.py` 连接OpenCompass和已校准Gemini协议，支持生成/评分分段及明确标注的小样本验证。旧脚本和实验记录保留原字节供历史核验，不改写已有实验来源。`--dry-run` 仅做真实数据准备与配置检查，不产生新的模型成绩。该阶段仅做本地检查；后续GPU实测已完成，结果见下节。

本轮验证：新增6项入口检查、60项相关回归检查通过；主控完整dry-run在 `logs/week8-entrypoints-20260919-final` 完成数据准备、SFT/DPO配置和全量评估配置生成。评分测试使用离线响应样例，验证CSV计算、同目录复用和损坏输入拒绝，不是新增模型成绩。无GPU启动、真实API请求或重新训练。

最终仓库整理作为提交前必做项，不能仅以“六类目录已经存在”判定可交付。建议保留现仓库为过程档案，从验证后的交付内容建立干净提交仓库；是否创建新的远端以及仓库名称，待本地成品可审阅时确定。全部周次统一周报告、README、代码/配置、图表和必要结果的命名及导航；最终主README只保留一组现行命令、成果总览和各周索引。调试修复过程、重复导出物和过期状态说明归入过程档案，不与现行报告并列。

整理应保持 `data/scripts/configs/models/logs/reports` 功能分区，并为Week1–8各设唯一报告入口。涉及历史证据的引用与脚本依赖，在新副本中同步改好、验证后再提交，不直接移动或删除现仓库文件。权重、密钥、缓存和中间检查点不进入Git；最终模型保留下载/存储与校验说明。此项是整理已有成果，不新增实验或扩大教师要求。

## 本轮限定范围真实评估结果（2026-09-19）

现行主控在320的新代码目录执行正式数据准备，并复用已有最终DPO模型进行每科1题的基准测试及完整custom20。初次精简传输包遗漏了历史数据处理源文件及数据依赖；补齐后先通过本地解压副本dry-run，再启动成功运行，失败日志保留。成功运行退出码0，GPU生成于13:48:11 UTC结束；证据下载校验后，13:52 UTC前确认320已关机。本地评分随后完成，恰好20次Gemini请求，无重复请求。

| 项目 | 本轮结果 | 解释 |
|---|---|---|
| 正式数据 | 1422训练／158验证；15个输出与冻结基准逐字节一致 | 数据入口通过 |
| C-Eval | 43/52，82.6923% | 每科首题的小样本，仅验证入口 |
| CMMLU | 54/67，80.5970% | 每科首题的小样本，仅验证入口 |
| custom20 | 20/20完整答案；五维加权均分3.775/5 | Gemini新评分；不覆盖已有三模型比较 |
| 核验 | 366个生成文件、305个依赖文件、答案EOS、参考答案及CSV复算均通过 | 无训练重复、无截断答案 |
| 环境 | 新代码目录；既有Linux CUDA环境；pip check通过并保留全量freeze | **不是从零安装CUDA环境的验收** |

[验证记录](../reports/week8/delivery_eval_20260919/verification.json)及[结果CSV](../reports/week8/delivery_eval_20260919/retrieved/eval-check-002/evaluation/summary.csv)为本轮依据。该结果补齐新入口的真实推理与评分路径证据，不把119题样本包装成12928题全量复现，也不据此声称效果提升或扩大上线。

此前计划进入检查点清理；用户后来明确暂不考虑删除，当前不执行该项。本轮只读盘点320共90个checkpoint目录，其中6个属于Week8的smoke、SFT、DPO和学生蒸馏；尚未删除任何目录。然后同步最终报告，统一各周报告入口与仓库目录，完成Git交付。仓库整理时需保留或同步修正数据协议引用的历史路径，不能只复制scripts/configs而遗漏输入依赖。

## 关键证据入口

- 教师原件：`实习需求.docx`；来源：`reports/week8/requirements_source.json`。
- 训练：`reports/week8/phase2_full_training_target/target_review.json`。
- 评测：`reports/week8/current_evaluation_summary.csv`。
- 部署：`reports/week8/phase4_supervised_deployment/checks.json`。
- 蒸馏：`reports/week8/phase3_distillation_student_gpu/checks.json`与`phase3_distillation_comparison/checks.json`。
- 报告：`reports/technical_report.md/tex/pdf`，当前检查见`reports/week8/latex_report_20260919/verification.json`；旧Word版收据及文件保留历史状态。
- 模型备份：`reports/week8/model_backup_20260919/verification.json`。
- 最近独立副本：`reports/week8/clean_eval_release_20260919/verification.json`，196项通过；该记录也明确fresh_model_evaluation_completed=false。
- Git：本次本地读取显示最新提交仍为3899d10（2026-08-31）；Week8代码等尚未提交。本轮未联网更新远端状态，不能据此断言远端没有其他人的更新。

## 检查点清理准备（2026-09-19）

320已通过无卡模式完成清理前核对。本地最终三模型38个文件重新通过SHA256校验，远端最终模型文件数量、大小、配置及索引一致；6个Week8检查点共1,362,166,523字节，训练元数据已保存，未发现活动训练/评估/部署任务。用户已明确暂不考虑删除；不再等待删除确认，尚未删除。[具体清单](../reports/week8/checkpoint_cleanup_20260919/review.md)及[当前状态](../reports/week8/checkpoint_cleanup_20260919/verification.json)。
